---
title: Namespace
description: Ein Namespace ist ein Kontext, in dem die Namen aller Objekte eindeutig aufgelöst werden müssen.
ms.assetid: 7731f6b5-1efa-43bc-bd31-9b5183ec90dc
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: eb79ba44bfaab1ea50dd89961503176dc8fbd1a7
ms.sourcegitcommit: 6515eef99ca0d1bbe3e27d4575e9986f5255f277
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 03/10/2021
ms.locfileid: "106355153"
---
# <a name="namespace"></a><span data-ttu-id="e4598-103">Namespace</span><span class="sxs-lookup"><span data-stu-id="e4598-103">Namespace</span></span>

<span data-ttu-id="e4598-104">Ein Namespace ist ein Kontext, in dem die Namen aller Objekte eindeutig aufgelöst werden müssen.</span><span class="sxs-lookup"><span data-stu-id="e4598-104">A namespace is a context within which the names of all objects must be unambiguously resolvable.</span></span> <span data-ttu-id="e4598-105">Das Internet ist beispielsweise ein einzelner DNS-Namespace, in dem alle Netzwerkgeräte mit einem DNS-Namen in eine bestimmte Adresse aufgelöst werden können (z `www.microsoft.com` . b. wird in 207.46.131.13 aufgelöst).</span><span class="sxs-lookup"><span data-stu-id="e4598-105">For example, the internet is a single DNS name space, within which all network devices with a DNS name can be resolved to a particular address (for example, `www.microsoft.com` resolves to 207.46.131.13).</span></span>

## <a name="flat-namespace"></a><span data-ttu-id="e4598-106">Flacher Namespace</span><span class="sxs-lookup"><span data-stu-id="e4598-106">Flat namespace</span></span>

<span data-ttu-id="e4598-107">Ein Namespace kann flach oder hierarchisch sein.</span><span class="sxs-lookup"><span data-stu-id="e4598-107">A namespace can be flat or hierarchical.</span></span> <span data-ttu-id="e4598-108">Ein flacher Namespace skaliert nicht gut, da er nur so groß werden kann, bis alle verfügbaren Namen aufgebraucht sind.</span><span class="sxs-lookup"><span data-stu-id="e4598-108">A flat namespace doesn't scale well because it can grow only so large before all available names are used up.</span></span> <span data-ttu-id="e4598-109">Sobald ein Name mehrmals in einem Namespace verwendet wird, verstößt der Namespace gegen die eindeutig auflösbare Anforderung.</span><span class="sxs-lookup"><span data-stu-id="e4598-109">Once a name is used more than once in a namespace, the namespace violates the unambiguously resolvable requirement.</span></span>

## <a name="hierarchical-namespace"></a><span data-ttu-id="e4598-110">Hierarchischer Namespace</span><span class="sxs-lookup"><span data-stu-id="e4598-110">Hierarchical namespace</span></span>

<span data-ttu-id="e4598-111">Ein hierarchischer Namespace ist in verschiedene Bereiche unterteilt, die als untergeordnete Namespaces betrachtet werden können.</span><span class="sxs-lookup"><span data-stu-id="e4598-111">A hierarchical namespace is divided into different areas, which can be thought of as sub-namespaces.</span></span> <span data-ttu-id="e4598-112">Jeder Bereich ist ein eigener untergeordneter Namespace innerhalb des gesamten Namespace.</span><span class="sxs-lookup"><span data-stu-id="e4598-112">Each area is its own sub-namespace within the overall namespace.</span></span> <span data-ttu-id="e4598-113">Daher muss jedes Objekt einen eindeutigen Namen nur innerhalb seines untergeordneten Namespace aufweisen, um einen eindeutig auflösbaren Namen innerhalb der Namespace Hierarchie zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="e4598-113">Therefore, each object must have a unique name only within its sub-namespace in order to have an unambiguously resolvable name within the namespace hierarchy.</span></span> <span data-ttu-id="e4598-114">Hierarchische Namespaces können dann auf extrem große Netzwerke &mdash; skaliert werden, wenn Sie dem gesamten Namespace weitere Objekte hinzufügen. Sie müssen nur innerhalb des untergeordneten Namespace, zu dem Sie gehören, eindeutige Namen finden.</span><span class="sxs-lookup"><span data-stu-id="e4598-114">Hierarchical namespaces, then, can scale to extremely large networks&mdash;as you add more objects to the overall name space, you have to find unique names for them within only the sub-namespace to which they belong.</span></span>

<span data-ttu-id="e4598-115">Alle DNS-Namespaces sind hierarchisch.</span><span class="sxs-lookup"><span data-stu-id="e4598-115">All DNS namespaces are hierarchical.</span></span> <span data-ttu-id="e4598-116">Die Subnamespaces im hierarchischen DNS-Namespace werden als *Domänen* bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="e4598-116">The sub-namespaces in the DNS hierarchical namespace are called *domains*.</span></span> <span data-ttu-id="e4598-117">Der eindeutige Name eines Computers in einer Domäne wird als *relativer Distinguished Name* bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="e4598-117">The unique name of a computer within a domain is called a *relative distinguished name*.</span></span> <span data-ttu-id="e4598-118">Computer mit demselben relativen Distinguished Name können in unterschiedlichen Subnamespaces (Domänen) der Namespace Hierarchie vorhanden sein, da Sie mithilfe eines voll qualifizierten Domänen Namens (FQDN) vollständig in ein eindeutiges Objekt innerhalb der gesamten DNS-Hierarchie aufgelöst werden können.</span><span class="sxs-lookup"><span data-stu-id="e4598-118">Computers with the same relative distinguished name can exist in different sub-namespaces (domains) of the namespace hierarchy because they can be fully resolved to a unique object within the entire DNS hierarchy, using a fully-qualified domain name (FQDN).</span></span> <span data-ttu-id="e4598-119">Beispielsweise können Sie einen Server mit dem Namen *Server1* in der *widgets.Microsoft.com* -Domäne (den *widgets.Microsoft.com* -Namespace) haben, und Sie können *Server1* im *Gadgets.widgets.Microsoft.com* -Namespace haben.</span><span class="sxs-lookup"><span data-stu-id="e4598-119">For example, you could have a server named *server1* in the *widgets.microsoft.com* domain (the *widgets.microsoft.com* namespace), and you could have *server1* in the *gadgets.widgets.microsoft.com* namespace.</span></span> <span data-ttu-id="e4598-120">Da Sie sich in verschiedenen Subnamespaces im hierarchischen Namespace befinden, können Sie in verschiedene FQDNs &mdash; -*Server1.widgets.Microsoft.com* und *Server1.Gadgets.widgets.Microsoft.com* aufgelöst werden.</span><span class="sxs-lookup"><span data-stu-id="e4598-120">Because they are in different sub-namespaces in the hierarchical namespace, they can be resolved to different FQDNs&mdash;*server1.widgets.microsoft.com* and *server1.gadgets.widgets.microsoft.com*.</span></span>
