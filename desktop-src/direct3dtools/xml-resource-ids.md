---
description: Vom Aufrufer festgelegte Ressourcenzeichenfolgen-IDs, die in XML-Daten zum Visualisieren von Objekten zurückgegeben werden sollen.
MS-HAID: vspixengine.Xml\_Resource\_Ids
MSHAttr:
- PreferredSiteName:MSDN
- PreferredLib:/library/windows/desktop
title: Xml_Resource_Ids-Enumeration
ms.topic: reference
ms.date: 05/31/2018
ms.assetid: 1F2ECC78-73EC-4A59-8CF7-1BB7FB80244E
api_name:
- Xml_Resource_Ids
api_type:
- HeaderDef
api_location:
- vspixengine.h
topic_type:
- APIRef
- kbSyntax
ms.openlocfilehash: 6cde2fe260924702eaeaebad235ebf1b469c73ed
ms.sourcegitcommit: c276a8912787b2cda74dcf54eb96df961bb1188b
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 08/20/2021
ms.locfileid: "122621906"
---
# <a name="span-idvspixenginexml_resource_idsspanxml_resource_ids-enumeration"></a><span id="vspixengine.xml_resource_ids"></span>Enumeration \_ von XML-Ressourcen-IDs \_

Vom Aufrufer festgelegte Ressourcenzeichenfolgen-IDs, die in XML-Daten zum Visualisieren von Objekten zurückgegeben werden sollen

## <a name="syntax"></a>Syntax


```C++
};
```

## <a name="constants"></a>Konstanten

<span id="XML_DEVICE_INFORMATION"></span><span id="xml_device_information"></span>**\_ \_ XML-GERÄTEINFORMATIONEN**  
Ressourcen-ID für die Zeichenfolge "Geräteinformationen", die in XML-Daten verwendet wird.

<span id="XML_IA_STATE"></span><span id="xml_ia_state"></span>**XML \_ IA \_ STATE**  
Ressourcen-ID für die Zeichenfolge "Eingabeassemblierer", die in XML-Daten verwendet wird.

<span id="XML_VS_STATE"></span><span id="xml_vs_state"></span>**XML \_ VS \_ STATE**  
Ressourcen-ID für die Zeichenfolge "Vertex-Shaderphase", die in XML-Daten verwendet wird.

<span id="XML_GS_STATE"></span><span id="xml_gs_state"></span>**XML \_ GS \_ STATE**  
Ressourcen-ID für die Zeichenfolge "geometry shader stage", die in XML-Daten verwendet wird.

<span id="XML_RASTERIZER_STATE"></span><span id="xml_rasterizer_state"></span>**\_XML-RASTERIZERSTATUS \_**  
Ressourcen-ID für die Zeichenfolge "rasterizer stage", die in XML-Daten verwendet wird.

<span id="XML_PS_STATE"></span><span id="xml_ps_state"></span>**XML \_ PS \_ STATE**  
Ressourcen-ID für die Zeichenfolge "Pixelshaderphase", die in XML-Daten verwendet wird.

<span id="XML_OM_STATE"></span><span id="xml_om_state"></span>**XML \_ OM \_ STATE**  
Ressourcen-ID für die Zeichenfolge "Ausgabezusammenführungsphase", die in XML-Daten verwendet wird.

<span id="XML_HS_STATE"></span><span id="xml_hs_state"></span>**XML \_ HS \_ STATE**  
Ressourcen-ID für die Zeichenfolge "Hüllen-Shaderphase", die in XML-Daten verwendet wird.

<span id="XML_DS_STATE"></span><span id="xml_ds_state"></span>**XML \_ DS \_ STATE**  
Ressourcen-ID für die Zeichenfolge "Domänen-Shaderphase", die in XML-Daten verwendet wird.

<span id="XML_CS_STATE"></span><span id="xml_cs_state"></span>**XML \_ CS \_ STATE**  
Ressourcen-ID für die Zeichenfolge "Compute-Shaderphase", die in XML-Daten verwendet wird.

<span id="XML_CLASS_INSTANCE_11"></span><span id="xml_class_instance_11"></span>**\_XML-KLASSENINSTANZ \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "Klasseninstanzen", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_10"></span><span id="xml_device_10"></span>**\_XML-GERÄT \_ 10**  
Ressourcen-ID für die zeichenfolge "device 10", die in XML-Daten verwendet wird.

<span id="XML_INPUT_LAYOUT_11"></span><span id="xml_input_layout_11"></span>**\_XML-EINGABELAYOUT \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "Eingabelayout 11", die in XML-Daten verwendet wird.

<span id="XML_SAMPLER_STATE_11"></span><span id="xml_sampler_state_11"></span>**XML \_ SAMPLER \_ STATE \_ 11**  
Ressourcen-ID für die Zeichenfolge "sampler state 11", die in XML-Daten verwendet wird.

<span id="XML_RASTER_STATE_11"></span><span id="xml_raster_state_11"></span>**XML \_ RASTER \_ STATE \_ 11**  
Ressourcen-ID für die Zeichenfolge "Rasterzustand 11", die in XML-Daten verwendet wird.

<span id="XML_BLEND_STATE_11"></span><span id="xml_blend_state_11"></span>**XML \_ BLEND \_ STATE \_ 11**  
Ressourcen-ID für die Zeichenfolge "Blend state 11", die in XML-Daten verwendet wird.

<span id="XML_DEPTH_STENCIL_STATE_11"></span><span id="xml_depth_stencil_state_11"></span>**\_ \_ XML-TIEFENSCHABLONENZUSTAND \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "Tiefenschablonenzustand 11", die in XML-Daten verwendet wird.

<span id="XML_UNORDERED_ACCESS_VIEW_11"></span><span id="xml_unordered_access_view_11"></span>**XML \_ UNORDERED \_ ACCESS VIEW \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "ungeordnete Zugriffsansicht 11", die in XML-Daten verwendet wird.

<span id="XML_SHADER_RESOURCE_VIEW_11"></span><span id="xml_shader_resource_view_11"></span>**\_ \_ XML-SHADER-RESSOURCENANSICHT \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "Shaderressourcenansicht 11", die in XML-Daten verwendet wird.

<span id="XML_DEPTH_STENCIL_VIEW_11"></span><span id="xml_depth_stencil_view_11"></span>**\_ \_ XML-TIEFENSCHABLONENANSICHT \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "Tiefenschablonenansicht 11", die in XML-Daten verwendet wird.

<span id="XML_RENDER_TARGET_VIEW_11"></span><span id="xml_render_target_view_11"></span>**\_ \_ XML-RENDERZIELANSICHT \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "Renderzielansicht 11", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_11"></span><span id="xml_device_11"></span>**\_XML-GERÄT \_ 11**  
Ressourcen-ID für die zeichenfolge "device 11", die in XML-Daten verwendet wird.

<span id="XML_SWAP_CHAIN"></span><span id="xml_swap_chain"></span>**\_ \_ XML-SWAPKETTE**  
Ressourcen-ID für die zeichenfolge "swap chain", die in XML-Daten verwendet wird.

<span id="XML_INPUT_LAYOUT_10"></span><span id="xml_input_layout_10"></span>**\_XML-EINGABELAYOUT \_ \_ 10**  
Ressourcen-ID für die Zeichenfolge "Eingabelayout 10", die in XML-Daten verwendet wird.

<span id="XML_SAMPLER_STATE_10"></span><span id="xml_sampler_state_10"></span>**XML \_ SAMPLER \_ STATE \_ 10**  
Ressourcen-ID für die zeichenfolge "sampler state 10", die in XML-Daten verwendet wird.

<span id="XML_RASTER_STATE_10"></span><span id="xml_raster_state_10"></span>**XML \_ RASTER \_ STATE \_ 10**  
Ressourcen-ID für die Zeichenfolge "Rasterzustand 10", die in XML-Daten verwendet wird.

<span id="XML_BLEND_STATE_10"></span><span id="xml_blend_state_10"></span>**XML \_ BLEND \_ STATE \_ 10**  
Ressourcen-ID für die Zeichenfolge "Blend state 10", die in XML-Daten verwendet wird.

<span id="XML_DEPTH_STENCIL_STATE_10"></span><span id="xml_depth_stencil_state_10"></span>**\_ \_ XML-TIEFENSCHABLONENZUSTAND \_ \_ 10**  
Ressourcen-ID für die Zeichenfolge "Tiefenschablonenzustand 10", die in XML-Daten verwendet wird.

<span id="XML_SHADER_RESOURCE_VIEW_10"></span><span id="xml_shader_resource_view_10"></span>**\_ \_ XML-SHADER-RESSOURCENANSICHT \_ \_ 10**  
Ressourcen-ID für die Zeichenfolge "Shaderressourcenansicht 10", die in XML-Daten verwendet wird.

<span id="XML_DEPTH_STENCIL_VIEW_10"></span><span id="xml_depth_stencil_view_10"></span>**\_ \_ XML-TIEFENSCHABLONENANSICHT \_ \_ 10**  
Ressourcen-ID für die Zeichenfolge "Tiefenschablonenansicht 10", die in XML-Daten verwendet wird.

<span id="XML_RENDER_TARGET_VIEW_10"></span><span id="xml_render_target_view_10"></span>**\_ \_ XML-RENDERZIELANSICHT \_ \_ 10**  
Ressourcen-ID für die Zeichenfolge "Renderzielansicht 10", die in XML-Daten verwendet wird.

<span id="XML_VERTEX_DECLARATION"></span><span id="xml_vertex_declaration"></span>**XML \_ VERTEX \_ DECLARATION**  
Ressourcen-ID für die Zeichenfolge "Scheitelpunktdeklaration", die in XML-Daten verwendet wird.

<span id="XML_STATE_BLOCK_STATE_10"></span><span id="xml_state_block_state_10"></span>**XML \_ STATE BLOCK STATE \_ \_ \_ 10**  
Ressourcen-ID für die Zeichenfolge "Zustandsblockzustand 10", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_CONTEXT_STATE_11"></span><span id="xml_device_context_state_11"></span>**\_ \_ XML-GERÄTEKONTEXTSTATUS \_ \_ 11**  
Ressourcen-ID für die Zeichenfolge "Gerätekontextzustand 11", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_STATE_10"></span><span id="xml_device_state_10"></span>**XML \_ DEVICE \_ STATE \_ 10**  
Ressourcen-ID für die Zeichenfolge "Gerätestatus 10", die in XML-Daten verwendet wird.

<span id="XML_BACK_FACE"></span><span id="xml_back_face"></span>**\_ \_ XML-RÜCKWAND**  
Ressourcen-ID für die zeichenfolge "back face", die in XML-Daten verwendet wird.

<span id="XML_FRONT_FACE"></span><span id="xml_front_face"></span>**XML \_ FRONT \_ FACE**  
Ressourcen-ID für die Zeichenfolge "front face", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_CREATION_INFORMATION"></span><span id="xml_device_creation_information"></span>**\_INFORMATIONEN \_ ZUR XML-GERÄTEERSTELLUNG \_**  
Ressourcen-ID für die Zeichenfolge "Geräteerstellungsinformationen", die in XML-Daten verwendet wird.

<span id="XML_OTHER_INFORMATION"></span><span id="xml_other_information"></span>**\_ANDERE \_ XML-INFORMATIONEN**  
Ressourcen-ID für die Zeichenfolge "andere Informationen", die in XML-Daten verwendet wird.

<span id="XML_MULTISAMPLING"></span><span id="xml_multisampling"></span>**XML \_ MULTISAMPLING**  
Ressourcen-ID für die zeichenfolge "multisampling", die in XML-Daten verwendet wird.

<span id="XML_BACK_BUFFER"></span><span id="xml_back_buffer"></span>**XML \_ BACK \_ BUFFER**  
Ressourcen-ID für die zeichenfolge "back buffer", die in XML-Daten verwendet wird.

<span id="XML_GENERAL_INFORMATION"></span><span id="xml_general_information"></span>**\_ALLGEMEINE \_ XML-INFORMATIONEN**  
Ressourcen-ID für die Zeichenfolge "allgemeine Informationen", die in XML-Daten verwendet wird.

<span id="XML_RENDER_TARGETS"></span><span id="xml_render_targets"></span>**\_ \_ XML-RENDERZIELE**  
Ressourcen-ID für die Zeichenfolge "Renderziele", die in XML-Daten verwendet wird.

<span id="XML_BLEND_DESCRIPTION"></span><span id="xml_blend_description"></span>**XML \_ BLEND \_ DESCRIPTION**  
Ressourcen-ID für die Zeichenfolge "Blend description", die in XML-Daten verwendet wird.

<span id="XML_DEPTH_STENCIL"></span><span id="xml_depth_stencil"></span>**\_ \_ XML-TIEFENSCHABLONE**  
Ressourcen-ID für die Zeichenfolge "Tiefenschablone", die in XML-Daten verwendet wird.

<span id="XML_FRONT_FACE_STENCIL"></span><span id="xml_front_face_stencil"></span>**XML \_ FRONT \_ FACE \_ STENCIL**  
Ressourcen-ID für die Zeichenfolge "Front-Face-Schablone", die in XML-Daten verwendet wird.

<span id="XML_BACK_FACE_STENCIL"></span><span id="xml_back_face_stencil"></span>**\_ \_ \_ XML-RÜCKWAND-SCHABLONE**  
Ressourcen-ID für die Zeichenfolge "Rückwandschablone", die in XML-Daten verwendet wird.

<span id="XML_DEVICE1_CREATION_INFORMATION"></span><span id="xml_device1_creation_information"></span>**XML \_ \_ DEVICE1-ERSTELLUNGSINFORMATIONEN \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "device1 creation information".

<span id="XML_VIEWPORTS"></span><span id="xml_viewports"></span>**XML \_ VIEWPORTS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "viewports".

<span id="XML_SCISSOR_RECTS"></span><span id="xml_scissor_rects"></span>**XML \_ SCISSOR \_ RECTS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "scissor rects".

<span id="XML_RASTERIZER"></span><span id="xml_rasterizer"></span>**\_XML-RASTERIZER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Rasterizer".

<span id="XML_SHADER_GENERAL_INFORMATION"></span><span id="xml_shader_general_information"></span>**ALLGEMEINE \_ INFORMATIONEN ZUM XML-SHADER \_ \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "shader general information".

<span id="XML_SHADER_RESOURCES"></span><span id="xml_shader_resources"></span>**\_XML-SHADERRESSOURCEN \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Shaderressourcen".

<span id="XML_SAMPLER_STATES"></span><span id="xml_sampler_states"></span>**XML \_ SAMPLER \_ STATES**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "sampler states".

<span id="XML_CONSTANT_BUFFERS"></span><span id="xml_constant_buffers"></span>**\_ \_ XML-KONSTANTENPUFFER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "constant buffers".

<span id="XML_STREAM_OUTPUT_BUFFERS"></span><span id="xml_stream_output_buffers"></span>**\_ \_ XML-STREAMAUSGABEPUFFER \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Streamausgabepuffer".

<span id="XML_INPUT_LAYOUT_GENERAL_INFORMATION"></span><span id="xml_input_layout_general_information"></span>**ALLGEMEINE \_ INFORMATIONEN ZUM \_ \_ XML-EINGABELAYOUT \_**  
Ressourcen-ID für die Zeichenfolge "Allgemeine Informationen zum Eingabelayout", die in XML-Daten verwendet wird.

<span id="XML_INPUT_LAYOUTS"></span><span id="xml_input_layouts"></span>**\_ \_ XML-EINGABELAYOUTS**  
Ressourcen-ID für Zeichenfolgen-Eingabelayouts, die in XML-Daten verwendet werden.

<span id="XML_VERTEX_BUFFERS"></span><span id="xml_vertex_buffers"></span>**\_XML-VERTEXPUFFER \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Scheitelpunktpuffer".

<span id="XML_INDEX_BUFFERS"></span><span id="xml_index_buffers"></span>**\_ \_ XML-INDEXPUFFER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Indexpuffer".

<span id="XML_PRIMITIVE_TOP_GENERAL"></span><span id="xml_primitive_top_general"></span>**XML \_ PRIMITIVE \_ TOP \_ GENERAL**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "primitive top general".

<span id="XML_BLEND_STATE"></span><span id="xml_blend_state"></span>**XML \_ BLEND \_ STATE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "blend state".

<span id="XML_UNORDERED_ACCESS_VIEWS"></span><span id="xml_unordered_access_views"></span>**\_UNGEORDNETE \_ XML-ZUGRIFFSANSICHTEN \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "ungeordnete Zugriffsansichten".

<span id="XML_CLASS_INSTANCES"></span><span id="xml_class_instances"></span>**\_ \_ XML-KLASSENINSTANZEN**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Klasseninstanzen".

<span id="XML_INSTANCE_INDEX"></span><span id="xml_instance_index"></span>**XML \_ INSTANCE \_ INDEX**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Instanzindex".

<span id="XML_INSTANCE_NAME"></span><span id="xml_instance_name"></span>**\_ \_ XML-INSTANZNAME**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Instanzname".

<span id="XML_BASE_SAMPLER"></span><span id="xml_base_sampler"></span>**XML \_ BASE \_ SAMPLER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "base sampler".

<span id="XML_BASE_TEXTURE"></span><span id="xml_base_texture"></span>**\_ \_ XML-BASISTEXTUR**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Basistextur".

<span id="XML_BASE_CONSTANT_BUFFER_OFFSET"></span><span id="xml_base_constant_buffer_offset"></span>**XML \_ BASE \_ CONSTANT \_ BUFFER \_ OFFSET**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Base Constant Buffer Offset".

<span id="XML_CONSTANT_BUFFER"></span><span id="xml_constant_buffer"></span>**XML \_ CONSTANT \_ BUFFER**  
Ressourcen-ID für die Zeichenfolge "konstanter Puffer", die in XML-Daten verwendet wird.

<span id="XML_TYPE_ID"></span><span id="xml_type_id"></span>**\_XML-TYP-ID \_**  
Ressourcen-ID für die Zeichenfolge "Typ-ID", die in XML-Daten verwendet wird.

<span id="XML_INSTANCE_ID"></span><span id="xml_instance_id"></span>**\_XML-INSTANZ-ID \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Instanz-ID".

<span id="XML_TYPE_NAME"></span><span id="xml_type_name"></span>**\_ \_ XML-TYPNAME**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Typname".

<span id="XML_CREATED_BY"></span><span id="xml_created_by"></span>**XML \_ CREATED \_ BY**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "created by".

<span id="XML_CLASS_LINKAGE"></span><span id="xml_class_linkage"></span>**\_ \_ XML-KLASSENVERKNÜPFUNG**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Klassenverknüpfung".

<span id="XML_DESCRIPTION"></span><span id="xml_description"></span>**XML \_ DESCRIPTION**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "description".

<span id="XML_SEMANTIC_NAME"></span><span id="xml_semantic_name"></span>**XML \_ SEMANTIC \_ NAME**  
Ressourcen-ID für die Zeichenfolge "semantischer Name", die in XML-Daten verwendet wird.

<span id="XML_SEMANTIC_INDEX"></span><span id="xml_semantic_index"></span>**XML \_ SEMANTIC \_ INDEX**  
Ressourcen-ID für die Zeichenfolge "semantischer Index", die in XML-Daten verwendet wird.

<span id="XML_FORMAT"></span><span id="xml_format"></span>**\_XML-FORMAT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "format".

<span id="XML_INPUT_SLOT"></span><span id="xml_input_slot"></span>**\_ \_ XML-EINGABESLOT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Eingabeslot".

<span id="XML_ALIGNED_BYTE_OFFSET"></span><span id="xml_aligned_byte_offset"></span>**\_XML-AUSGERICHTETER \_ \_ BYTEOFFSET**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "aligned byte offset".

<span id="XML_INPUT_SLOT_CLASS"></span><span id="xml_input_slot_class"></span>**XML \_ INPUT \_ \_ SLOT-KLASSE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Eingabeslotklasse".

<span id="XML_INSTANCE_DATA_STEP_RATE"></span><span id="xml_instance_data_step_rate"></span>**\_DATENSCHRITTRATE \_ DER \_ \_ XML-INSTANZ**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Instanzdatenschrittrate".

<span id="XML_ADDRESS_U"></span><span id="xml_address_u"></span>**\_XML-ADRESSE \_ U**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "address u".

<span id="XML_ADDRESS_V"></span><span id="xml_address_v"></span>**\_XML-ADRESSE \_ V**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "address v".

<span id="XML_ADDRESS_W"></span><span id="xml_address_w"></span>**\_XML-ADRESSE \_ W**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "address w".

<span id="XML_MIN_LOD"></span><span id="xml_min_lod"></span>**XML \_ MIN \_ LOD**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "min lod".

<span id="XML_MAX_LOD"></span><span id="xml_max_lod"></span>**XML \_ MAX \_ LOD**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "max lod".

<span id="XML_FILTER"></span><span id="xml_filter"></span>**\_XML-FILTER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "filter".

<span id="XML_MIP_SLICE"></span><span id="xml_mip_slice"></span>**XML \_ MIP \_ SLICE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "mip slice".

<span id="XML_MOST_DETAILED_MIP"></span><span id="xml_most_detailed_mip"></span>**XML \_ MOST \_ DETAILED \_ MIP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "most detailed mip".

<span id="XML_MIP_LEVELS"></span><span id="xml_mip_levels"></span>**\_XML-MIP-EBENEN \_**  
Ressourcen-ID für Zeichenfolgen-"Mip-Ebenen", die in XML-Daten verwendet werden.

<span id="XML_FIRST_ARRAY_SLICE"></span><span id="xml_first_array_slice"></span>**XML \_ FIRST \_ ARRAY \_ SLICE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "first array slice".

<span id="XML_ARRAY_SIZE"></span><span id="xml_array_size"></span>**GRÖßE DES \_ \_ XML-ARRAYS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Arraygröße".

<span id="XML_RESOURCE"></span><span id="xml_resource"></span>**\_XML-RESSOURCE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "resource".

<span id="XML_RESOURCE_TYPE"></span><span id="xml_resource_type"></span>**\_ \_ XML-RESSOURCENTYP**  
Ressourcen-ID für die Zeichenfolge "Ressourcentyp", die in XML-Daten verwendet wird.

<span id="XML_ELEMENT_OFFSET"></span><span id="xml_element_offset"></span>**\_ \_ XML-ELEMENTOFFSET**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Elementoffset".

<span id="XML_ELEMENT_WIDTH"></span><span id="xml_element_width"></span>**\_ \_ XML-ELEMENTBREITE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Elementbreite".

<span id="XML_FIRST_WITH_SLICE"></span><span id="xml_first_with_slice"></span>**XML \_ FIRST \_ WITH \_ SLICE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "first with slice".

<span id="XML_BORDER_COLOR"></span><span id="xml_border_color"></span>**\_ \_ XML-RAHMENFARBE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Border Color".

<span id="XML_SLOT"></span><span id="xml_slot"></span>**\_XML-SLOT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "slot".

<span id="XML_COMPARISON_FUNCTION"></span><span id="xml_comparison_function"></span>**\_XML-VERGLEICHSFUNKTION \_**  
Ressourcen-ID für die in XML-Daten verwendete "Vergleichsfunktion" der Zeichenfolge.

<span id="XML_MAX_ANISOTROPY"></span><span id="xml_max_anisotropy"></span>**MAX. \_ \_ XML-ANISOTROPIE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "max anisotropy".

<span id="XML_MIP_LOD_BIAS"></span><span id="xml_mip_lod_bias"></span>**XML \_ MIP \_ LOD \_ BIAS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "mip lod bias".

<span id="XML_FILL_MODE"></span><span id="xml_fill_mode"></span>**\_XML-FÜLLMODUS \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Füllmodus".

<span id="XML_CULL_MODE"></span><span id="xml_cull_mode"></span>**XML \_ \_ CULL-MODUS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "cull mode".

<span id="XML_FRONT_COUNTER_CLOCKWISE"></span><span id="xml_front_counter_clockwise"></span>**XML \_ FRONT \_ COUNTER \_ CLOCKWISE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "front counter clockwise".

<span id="XML_DEPTH_BIAS"></span><span id="xml_depth_bias"></span>**XML \_ DEPTH \_ BIAS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Tiefenvoreingenommenheit".

<span id="XML_DEPTH_BIAS_CLAMP"></span><span id="xml_depth_bias_clamp"></span>**XML \_ DEPTH \_ \_ BIAS-KLAMMER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Depth Bias Clamp".

<span id="XML_SLOPE_SCALE_DEPTH_BIAS"></span><span id="xml_slope_scale_depth_bias"></span>**ABWEICHUNG DER \_ \_ XML-STEIGUNGSSKALA \_ \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Steigungsskala-Tiefenvoreingenommenheit".

<span id="XML_DEPTH_CLIP_ENABLE"></span><span id="xml_depth_clip_enable"></span>**XML \_ DEPTH \_ CLIP \_ ENABLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "depth clip enable".

<span id="XML_SCISSOR_ENABLE"></span><span id="xml_scissor_enable"></span>**XML \_ SCISSOR \_ ENABLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "scissor enable".

<span id="XML_MULTISAMPLE_ENABLE"></span><span id="xml_multisample_enable"></span>**XML \_ MULTISAMPLE \_ ENABLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "multisample enable".

<span id="XML_ANTIALIASED_LINE_ENABLE"></span><span id="xml_antialiased_line_enable"></span>**XML \_ ANTIALIASED \_ LINE \_ ENABLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "antialiased line enable".

<span id="XML_FUNCTION"></span><span id="xml_function"></span>**\_XML-FUNKTION**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "function".

<span id="XML_PASS_OP"></span><span id="xml_pass_op"></span>**XML \_ PASS \_ OP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "pass op".

<span id="XML_FAIL_OP"></span><span id="xml_fail_op"></span>**XML \_ FAIL \_ OP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "fail op".

<span id="XML_DEPTH_FAIL_OP"></span><span id="xml_depth_fail_op"></span>**XML \_ DEPTH \_ FAIL \_ OP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "depth fail op".

<span id="XML_DEPTH_ENABLE"></span><span id="xml_depth_enable"></span>**XML \_ DEPTH \_ ENABLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "depth enable".

<span id="XML_DEPTH_FUNCTION"></span><span id="xml_depth_function"></span>**\_ \_ XML-TIEFENFUNKTION**  
Ressourcen-ID für die in XML-Daten verwendete "Tiefenfunktion" der Zeichenfolge.

<span id="XML_DEPTH_WRITE_MASK"></span><span id="xml_depth_write_mask"></span>**XML \_ DEPTH \_ WRITE \_ MASK**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Tiefen-Schreibmaske".

<span id="XML_STENCIL_ENABLE"></span><span id="xml_stencil_enable"></span>**AKTIVIEREN \_ DER XML-SCHABLONE \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "stencil enable".

<span id="XML_STENCIL_READ_MASK"></span><span id="xml_stencil_read_mask"></span>**\_ \_ XML-SCHABLONENLESEMASKE \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Schablonenlesemaske".

<span id="XML_STENCIL_WRITE_MASK"></span><span id="xml_stencil_write_mask"></span>**\_ \_ XML-SCHABLONEN-SCHREIBMASKE \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Schablonen-Schreibmaske".

<span id="XML_FIRST_ELEMENT"></span><span id="xml_first_element"></span>**XML \_ \_ FIRST-ELEMENT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "first element".

<span id="XML_NUM_ELEMENTS"></span><span id="xml_num_elements"></span>**XML \_ \_ NUM-ELEMENTE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "num elements".

<span id="XML_FLAGS"></span><span id="xml_flags"></span>**\_XML-FLAGS**  
Ressourcen-ID für Zeichenfolgenflags, die in XML-Daten verwendet werden.

<span id="XML_W_SIZE"></span><span id="xml_w_size"></span>**XML \_ W \_ SIZE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "w size".

<span id="XML_FIRST_2D_ARRAY_FACE"></span><span id="xml_first_2d_array_face"></span>**XML \_ FIRST \_ 2D \_ ARRAY \_ FACE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "first 2d array face".

<span id="XML_NUM_CUBES"></span><span id="xml_num_cubes"></span>**XML \_ NUM \_ CUBES**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "num cubes".

<span id="XML_FACTORY"></span><span id="xml_factory"></span>**XML \_ FACTORY**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "factory".

<span id="XML_ADAPTER"></span><span id="xml_adapter"></span>**\_XML-ADAPTER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "adapter".

<span id="XML_HEIGHT"></span><span id="xml_height"></span>**XML \_ HEIGHT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "height".

<span id="XML_WIDTH"></span><span id="xml_width"></span>**\_XML-BREITE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "width".

<span id="XML_CREATION_FLAGS"></span><span id="xml_creation_flags"></span>**\_ \_ XML-ERSTELLUNGSFLAGS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Erstellungsflags".

<span id="XML_SDK_VERSION"></span><span id="xml_sdk_version"></span>**XML \_ \_ SDK-VERSION**  
Ressourcen-ID für die Zeichenfolge "SDK-Version", die in XML-Daten verwendet wird.

<span id="XML_DRIVER_TYPE"></span><span id="xml_driver_type"></span>**\_ \_ XML-TREIBERTYP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Treibertyp".

<span id="XML_FEATURE_LEVEL"></span><span id="xml_feature_level"></span>**\_ \_ XML-FEATUREEBENE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Featureebene".

<span id="XML_IMMEDIATE_CONTEXT"></span><span id="xml_immediate_context"></span>**XML \_ IMMEDIATE \_ CONTEXT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "immediate context".

<span id="XML_EXCEPTION_MODE"></span><span id="xml_exception_mode"></span>**\_ \_ XML-AUSNAHMEMODUS**  
Ressourcen-ID für die Zeichenfolge "Ausnahmemodus", die in XML-Daten verwendet wird.

<span id="XML_REFRESH_RATE"></span><span id="xml_refresh_rate"></span>**\_ \_ XML-AKTUALISIERUNGSRATE**  
Ressourcen-ID für die in XML-Daten verwendete "Aktualisierungsrate" der Zeichenfolge.

<span id="XML_SCANLINE_ORDERING"></span><span id="xml_scanline_ordering"></span>**XML \_ SCANLINE \_ ORDERING**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "scanline ordering".

<span id="XML_SCALING"></span><span id="xml_scaling"></span>**\_XML-SKALIERUNG**  
Ressourcen-ID für die in XML-Daten verwendete "Skalierung" der Zeichenfolge.

<span id="XML_USAGE"></span><span id="xml_usage"></span>**\_XML-VERWENDUNG**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "usage".

<span id="XML_BUFFER_COUNT"></span><span id="xml_buffer_count"></span>**\_ \_ XML-PUFFERANZAHL**  
Ressourcen-ID für die in XML-Daten verwendete "Pufferanzahl" der Zeichenfolge.

<span id="XML_WINDOWED"></span><span id="xml_windowed"></span>**XML \_ WINDOWED**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "windowed".

<span id="XML_SWAP_EFFECT"></span><span id="xml_swap_effect"></span>**XML \_ SWAP \_ EFFECT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "swap effect".

<span id="XML_STREAM"></span><span id="xml_stream"></span>**XML \_ STREAM**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "stream".

<span id="XML_OFFSET"></span><span id="xml_offset"></span>**\_XML-OFFSET**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "offset".

<span id="XML_TYPE"></span><span id="xml_type"></span>**\_XML-TYP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "type".

<span id="XML_METHOD"></span><span id="xml_method"></span>**\_XML-METHODE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "method".

<span id="XML_USAGE_INDEX"></span><span id="xml_usage_index"></span>**XML \_ USAGE \_ INDEX**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Usage index".

<span id="XML_BLEND_SOURCE"></span><span id="xml_blend_source"></span>**XML \_ BLEND \_ SOURCE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "blend source".

<span id="XML_BLEND_DESTINATION"></span><span id="xml_blend_destination"></span>**XML \_ BLEND \_ DESTINATION**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "blend destination".

<span id="XML_BLEND_OP"></span><span id="xml_blend_op"></span>**XML \_ BLEND \_ OP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "blend op".

<span id="XML_ALPHA_BLEND_SOURCE"></span><span id="xml_alpha_blend_source"></span>**XML \_ ALPHA \_ BLEND \_ SOURCE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "alpha blend source".

<span id="XML_ALPHA_BLEND_DEST"></span><span id="xml_alpha_blend_dest"></span>**XML \_ ALPHA \_ BLEND \_ DEST**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "alpha blend dest".

<span id="XML_ALPHA_BLEND_OP"></span><span id="xml_alpha_blend_op"></span>**XML \_ ALPHA \_ BLEND \_ OP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "alpha blend op".

<span id="XML_RENDER_TARGET_WRITE_MASK"></span><span id="xml_render_target_write_mask"></span>**XML \_ RENDER \_ TARGET \_ WRITE \_ MASK**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Renderziel-Schreibmaske".

<span id="XML_BLEND_ENABLE"></span><span id="xml_blend_enable"></span>**XML \_ BLEND \_ ENABLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "blend enable".

<span id="XML_ALPHA_TO_COVERAGE_ENABLE"></span><span id="xml_alpha_to_coverage_enable"></span>**AKTIVIEREN VON XML \_ ALPHA \_ TO \_ COVERAGE \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "alpha to coverage enable".

<span id="XML_BLEND_STATE_1"></span><span id="xml_blend_state_1"></span>**XML \_ BLEND \_ STATE \_ 1**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "blend state 1".

<span id="XML_INDEPENDENT_BLEND_ENABLE"></span><span id="xml_independent_blend_enable"></span>**XML \_ INDEPENDENT \_ BLEND \_ ENABLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "independent blend enable".

<span id="XML_RENDER_TARGET_NUMBER"></span><span id="xml_render_target_number"></span>**\_ \_ XML-RENDERZIELNUMMER \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Renderzielnummer".

<span id="XML_BUFFER"></span><span id="xml_buffer"></span>**XML \_ BUFFER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "buffer".

<span id="XML_PRIMITIVE_TOPOLOGY"></span><span id="xml_primitive_topology"></span>**PRIMITIVE \_ \_ XML-TOPOLOGIE**  
Ressourcen-ID für die Zeichenfolge "primitive Topologie", die in XML-Daten verwendet wird.

<span id="XML_STRIDE"></span><span id="xml_stride"></span>**\_XML-STRIDE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "stride".

<span id="XML_TOP"></span><span id="xml_top"></span>**XML \_ TOP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "top".

<span id="XML_RIGHT"></span><span id="xml_right"></span>**XML \_ RIGHT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "right".

<span id="XML_BOTTOM"></span><span id="xml_bottom"></span>**XML \_ BOTTOM**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "bottom".

<span id="XML_LEFT"></span><span id="xml_left"></span>**XML \_ LEFT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "left".

<span id="XML_TOP_LEFT_X"></span><span id="xml_top_left_x"></span>**XML \_ TOP \_ LEFT \_ X**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "top left x".

<span id="XML_TOP_LEFT_Y"></span><span id="xml_top_left_y"></span>**XML \_ TOP \_ LEFT \_ Y**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "top left y".

<span id="XML_MIN_DEPTH"></span><span id="xml_min_depth"></span>**XML \_ MIN \_ DEPTH**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "min depth".

<span id="XML_MAX_DEPTH"></span><span id="xml_max_depth"></span>**MAXIMALE \_ \_ XML-TIEFE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "max depth".

<span id="XML_STENCIL_REFERENCE_VALUE"></span><span id="xml_stencil_reference_value"></span>**\_ \_ XML-SCHABLONENVERWEISWERT \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Schablonenverweiswert".

<span id="XML_DEPTH_BUFFER"></span><span id="xml_depth_buffer"></span>**\_ \_ XML-TIEFENPUFFER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Tiefenpuffer".

<span id="XML_BLEND_FACTOR"></span><span id="xml_blend_factor"></span>**XML \_ BLEND \_ FACTOR**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "blend factor".

<span id="XML_SAMPLE_MASK"></span><span id="xml_sample_mask"></span>**XML \_ SAMPLE \_ MASK**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Sample Mask".

<span id="XML_DEPTH_STENCIL_STATE"></span><span id="xml_depth_stencil_state"></span>**\_ \_ XML-TIEFEN-SCHABLONENZUSTAND \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Tiefen-Schablonenzustand".

<span id="XML_SCISSOR_RECT"></span><span id="xml_scissor_rect"></span>**XML \_ SCISSOR \_ RECT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "scissor rect".

<span id="XML_SAMPLER"></span><span id="xml_sampler"></span>**XML \_ SAMPLER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "sampler".

<span id="XML_SHADER"></span><span id="xml_shader"></span>**\_XML-SHADER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Shader".

<span id="XML_DEPTH_STENCIL_VIEW"></span><span id="xml_depth_stencil_view"></span>**\_ \_ XML-TIEFEN-SCHABLONENANSICHT \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Tiefen-Schablonenansicht".

<span id="XML_SHADER_RESOURCE_VIEW"></span><span id="xml_shader_resource_view"></span>**\_ \_ XML-SHADER-RESSOURCENANSICHT \_**  
Ressourcen-ID für die Zeichenfolge "Shaderressourcenansicht", die in XML-Daten verwendet wird.

<span id="XML_INPUT_LAYOUT"></span><span id="xml_input_layout"></span>**\_ \_ XML-EINGABELAYOUT**  
Ressourcen-ID für die Zeichenfolge "Eingabelayout", die in XML-Daten verwendet wird.

<span id="XML_CLASS_INSTANCE"></span><span id="xml_class_instance"></span>**\_ \_ XML-KLASSENINSTANZ**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Klasseninstanz".

<span id="XML_STREAM_OUTPUT_BUFFER"></span><span id="xml_stream_output_buffer"></span>**XML \_ STREAM \_ OUTPUT \_ BUFFER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Streamausgabepuffer".

<span id="XML_SHADER_RESOURCE"></span><span id="xml_shader_resource"></span>**\_ \_ XML-SHADERRESSOURCE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Shaderressource".

<span id="XML_RENDER_TARGET_INSTANCE"></span><span id="xml_render_target_instance"></span>**\_ \_ XML-RENDERZIELINSTANZ \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Renderzielinstanz".

<span id="XML_VERTEX"></span><span id="xml_vertex"></span>**XML \_ VERTEX**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "vertex".

<span id="XML_VERTEX_BUFFER"></span><span id="xml_vertex_buffer"></span>**XML \_ VERTEX \_ BUFFER**  
Ressourcen-ID für die Zeichenfolge "Scheitelpunktpuffer", die in XML-Daten verwendet wird.

<span id="XML_INSTANCE"></span><span id="xml_instance"></span>**\_XML-INSTANZ**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "instanz".

<span id="XML_VIEWPORT"></span><span id="xml_viewport"></span>**XML \_ VIEWPORT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "viewport".

<span id="XML_FIRSTCONSTANT"></span><span id="xml_firstconstant"></span>**XML \_ FIRSTCONSTANT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "firstconstant".

<span id="XML_NUMCONSTANTS"></span><span id="xml_numconstants"></span>**XML \_ NUMCONSTANTS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "numconstants".

<span id="XML_LOGIC_OP"></span><span id="xml_logic_op"></span>**XML \_ LOGIC \_ OP**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "logic op".

<span id="XML_LOGICOPS_ENABLE"></span><span id="xml_logicops_enable"></span>**XML \_ LOGICOPS \_ ENABLE**  
Ressourcen-ID für die Zeichenfolge "logicops enable", die in XML-Daten verwendet wird.

<span id="XML_SHADER_FILE"></span><span id="xml_shader_file"></span>**\_XML-SHADERDATEI \_**  
Ressourcen-ID für die Zeichenfolge "Shaderdatei", die in XML-Daten verwendet wird.

<span id="XML_FILE_NAME"></span><span id="xml_file_name"></span>**\_ \_ XML-DATEINAME**  
Ressourcen-ID für die Zeichenfolge "Dateiname", die in XML-Daten verwendet wird.

<span id="XML_10TEXTURE1D_DESC"></span><span id="xml_10texture1d_desc"></span>**XML \_ 10TEXTURE1D \_ DESC**  
Ressourcen-ID für die Zeichenfolge "10texture1d desc", die in XML-Daten verwendet wird.

<span id="XML_10TEXTURE2D_DESC"></span><span id="xml_10texture2d_desc"></span>**XML \_ 10TEXTURE2D \_ DESC**  
Ressourcen-ID für die Zeichenfolge "10texture2d desc", die in XML-Daten verwendet wird.

<span id="XML_10TEXTURE3D_DESC"></span><span id="xml_10texture3d_desc"></span>**XML \_ 10TEXTURE3D \_ DESC**  
Ressourcen-ID für die Zeichenfolge "10texture3d desc", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_ARRAYSIZE"></span><span id="xml_texture_arraysize"></span>**XML \_ TEXTURE \_ ARRAYSIZE**  
Ressourcen-ID für die Zeichenfolge "texture arraysize", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_MIPLEVEL"></span><span id="xml_texture_miplevel"></span>**XML \_ TEXTURE \_ MIPLEVEL**  
Ressourcen-ID für die Zeichenfolge "texture miplevel", die in XML-Daten verwendet wird.

<span id="XML_CPUFLAGS"></span><span id="xml_cpuflags"></span>**XML \_ CPUFLAGS**  
Ressourcen-ID für die Zeichenfolge "cpuflags", die in XML-Daten verwendet wird.

<span id="XML_MISCFLAGS"></span><span id="xml_miscflags"></span>**XML \_ MISCFLAGS**  
Ressourcen-ID für die Zeichenfolge "miscflags", die in XML-Daten verwendet wird.

<span id="XML_BINDFLAGS"></span><span id="xml_bindflags"></span>**XML \_ BINDFLAGS**  
Ressourcen-ID für die Zeichenfolge "bindflags", die in XML-Daten verwendet wird.

<span id="XML_SAMPLEDESC_QUALITY"></span><span id="xml_sampledesc_quality"></span>**XML \_ SAMPLEDESC \_ QUALITY**  
Ressourcen-ID für die Zeichenfolge "sampledesc quality", die in XML-Daten verwendet wird.

<span id="XML_SAMPLEDESC_COUNT"></span><span id="xml_sampledesc_count"></span>**XML \_ SAMPLEDESC \_ COUNT**  
Ressourcen-ID für die Zeichenfolge "sampledesc count", die in XML-Daten verwendet wird.

<span id="XML_DEPTH"></span><span id="xml_depth"></span>**\_XML-TIEFE**  
Ressourcen-ID für die zeichenfolge "depth", die in XML-Daten verwendet wird.

<span id="XML_11TEXTURE1D_DESC"></span><span id="xml_11texture1d_desc"></span>**XML \_ 11TEXTURE1D \_ DESC**  
Ressourcen-ID für die Zeichenfolge "11texture1d desc", die in XML-Daten verwendet wird.

<span id="XML_11TEXTURE2D_DESC"></span><span id="xml_11texture2d_desc"></span>**XML \_ 11TEXTURE2D \_ DESC**  
Ressourcen-ID für die Zeichenfolge "11texture2d desc", die in XML-Daten verwendet wird.

<span id="XML_11TEXTURE3D_DESC"></span><span id="xml_11texture3d_desc"></span>**XML \_ 11TEXTURE3D \_ DESC**  
Ressourcen-ID für die Zeichenfolge "11texture3d desc", die in XML-Daten verwendet wird.

<span id="XML_BYTEWIDTH"></span><span id="xml_bytewidth"></span>**XML \_ BYTEWIDTH**  
Ressourcen-ID für die Zeichenfolge "bytewidth", die in XML-Daten verwendet wird.

<span id="XML_10BUFFER_DESC"></span><span id="xml_10buffer_desc"></span>**XML \_ 10BUFFER \_ DESC**  
Ressourcen-ID für die Zeichenfolge "10buffer desc", die in XML-Daten verwendet wird.

<span id="XML_11BUFFER_DESC"></span><span id="xml_11buffer_desc"></span>**XML \_ 11BUFFER \_ DESC**  
Ressourcen-ID für die Zeichenfolge "11buffer desc", die in XML-Daten verwendet wird.

<span id="XML_BYTESTRIDE"></span><span id="xml_bytestride"></span>**XML \_ BYTESTRIDE**  
Ressourcen-ID für die Zeichenfolge "bytestride", die in XML-Daten verwendet wird.

<span id="XML_STEREO"></span><span id="xml_stereo"></span>**XML \_ STEREO**  
Ressourcen-ID für die zeichenfolge "stereo", die in XML-Daten verwendet wird.

<span id="XML_BACKGROUNDCOLOR"></span><span id="xml_backgroundcolor"></span>**XML \_ BACKGROUNDCOLOR**  
Ressourcen-ID für die Zeichenfolge "backgroundcolor", die in XML-Daten verwendet wird.

<span id="XML_ALPHA_MODE"></span><span id="xml_alpha_mode"></span>**\_ \_ XML-ALPHAMODUS**  
Ressourcen-ID für die Zeichenfolge "Alphamodus", die in XML-Daten verwendet wird.

<span id="XML_RESTRICTTOOUTPUT"></span><span id="xml_restricttooutput"></span>**XML \_ RESTRICTTOOUTPUT**  
Ressourcen-ID für die zeichenfolge "restricttooutput", die in XML-Daten verwendet wird.

<span id="XML_MODEROTATION"></span><span id="xml_moderotation"></span>**\_XML-MODUSROTATION**  
Ressourcen-ID für die Zeichenfolge "moderotation", die in XML-Daten verwendet wird.

<span id="XML_ISTEMPORARYMONOSUPPORTED"></span><span id="xml_istemporarymonosupported"></span>**XML \_ ISTEMPORARYMONOSUPPORTED**  
Ressourcen-ID für die Zeichenfolge "istemporarymonosupported", die in XML-Daten verwendet wird.

<span id="XML_FORCED_SAMPLE_COUNT"></span><span id="xml_forced_sample_count"></span>**ANZAHL \_ VON ERZWUNGENEN XML-BEISPIELEN \_ \_**  
Ressourcen-ID für die Zeichenfolge "erzwungene Stichprobenanzahl", die in XML-Daten verwendet wird.

<span id="XML_QUERY"></span><span id="xml_query"></span>**XML \_ QUERY**  
Ressourcen-ID für die zeichenfolge "query", die in XML-Daten verwendet wird.

<span id="XML_10QUERY_DESC"></span><span id="xml_10query_desc"></span>**XML \_ 10QUERY \_ DESC**  
Ressourcen-ID für die Zeichenfolge "10query desc", die in XML-Daten verwendet wird.

<span id="XML_11QUERY_DESC"></span><span id="xml_11query_desc"></span>**XML \_ 11QUERY \_ DESC**  
Ressourcen-ID für die zeichenfolge "11query desc", die in XML-Daten verwendet wird.

<span id="XML_FLAGS_NONE"></span><span id="xml_flags_none"></span>**\_XML-FLAGS \_ NONE**  
Ressourcen-ID für die Zeichenfolge "flags none", die in XML-Daten verwendet wird.

<span id="XML_BUFFERUSAGE"></span><span id="xml_bufferusage"></span>**XML \_ BUFFERUSAGE**  
Ressourcen-ID für die Zeichenfolge "bufferusage", die in XML-Daten verwendet wird.

<span id="XML_UNKNOWN"></span><span id="xml_unknown"></span>**XML \_ UNKNOWN**  
Ressourcen-ID für die zeichenfolge "unknown", die in XML-Daten verwendet wird.

<span id="XML_MAXFRAMELATENCY"></span><span id="xml_maxframelatency"></span>**XML \_ MAXFRAMELATENCY**  
Ressourcen-ID für die Zeichenfolge "maxframelatency", die in XML-Daten verwendet wird.

<span id="XML_SOURCESIZEWIDTH"></span><span id="xml_sourcesizewidth"></span>**XML \_ SOURCESIZEWIDTH**  
Ressourcen-ID für die Zeichenfolge "sourcesizewidth", die in XML-Daten verwendet wird.

<span id="XML_SOURCESIZEHEIGHT"></span><span id="xml_sourcesizeheight"></span>**XML \_ SOURCESIZEHEIGHT**  
Ressourcen-ID für die Zeichenfolge "sourcesizeheight", die in XML-Daten verwendet wird.

<span id="XML_MATRIXTRANSFORM"></span><span id="xml_matrixtransform"></span>**XML \_ MATRIXTRANSFORM**  
Ressourcen-ID für die Zeichenfolge "matrixtransform", die in XML-Daten verwendet wird.

<span id="XML_TILED_RESOURCE_INFO"></span><span id="xml_tiled_resource_info"></span>**\_ \_ \_ XML-KACHELRESSOURCENINFORMATIONEN**  
Ressourcen-ID für die Zeichenfolge "Kachelressourceninformationen", die in XML-Daten verwendet wird.

<span id="XML_TILES_RESOURCE_SUBRESOURCE_TILINGS"></span><span id="xml_tiles_resource_subresource_tilings"></span>**\_ \_ \_ RESSOURCENUNTERRESSOURCENKACHELN IN \_ XML-KACHELN**  
Ressourcen-ID für die Zeichenfolge "kacheln resource subresource tilings", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_CHILD_INFORMATION"></span><span id="xml_device_child_information"></span>**\_UNTERGEORDNETE \_ \_ XML-GERÄTEINFORMATIONEN**  
Ressourcen-ID für die Zeichenfolge "Untergeordnete Geräteinformationen", die in XML-Daten verwendet wird.

<span id="XML_DEVICE"></span><span id="xml_device"></span>**\_XML-GERÄT**  
Ressourcen-ID für die Zeichenfolge "device", die in XML-Daten verwendet wird.

<span id="XML_OTHER"></span><span id="xml_other"></span>**XML \_ OTHER**  
Ressourcen-ID für die Zeichenfolge "other", die in XML-Daten verwendet wird.

<span id="XML_PREDICATE"></span><span id="xml_predicate"></span>**\_XML-PRÄDIKAT**  
Ressourcen-ID für die zeichenfolge "predicate", die in XML-Daten verwendet wird.

<span id="XML_PREDICATE_VALUE"></span><span id="xml_predicate_value"></span>**\_XML-PRÄDIKATWERT \_**  
Ressourcen-ID für die Zeichenfolge "Prädikatwert", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_CONTEXT_TYPE"></span><span id="xml_device_context_type"></span>**\_ \_ XML-GERÄTEKONTEXTTYP \_**  
Ressourcen-ID für die Zeichenfolge "Gerätekontexttyp", die in XML-Daten verwendet wird.

<span id="XML_BUFFER_HANDLE"></span><span id="xml_buffer_handle"></span>**XML \_ BUFFER \_ HANDLE**  
Ressourcen-ID für die Zeichenfolge "Pufferhandle", die in XML-Daten verwendet wird.

<span id="XML_DEVICE_12"></span><span id="xml_device_12"></span>**\_XML-GERÄT \_ 12**  
Ressourcen-ID für die zeichenfolge "device 12", die in XML-Daten verwendet wird.

<span id="XML_MAX_RECORDING_COMMAND_LISTS"></span><span id="xml_max_recording_command_lists"></span>**XML \_ MAX \_ \_ RECORDING-BEFEHLSLISTEN \_**  
Ressourcen-ID für die Zeichenfolge "Max. Aufzeichnungsbefehlslisten", die in XML-Daten verwendet wird.

<span id="XML_DEFAULT_COMMAND_QUEUE"></span><span id="xml_default_command_queue"></span>**\_ \_ \_ XML-STANDARDBEFEHLSWARTESCHLANGE**  
Ressourcen-ID für die Zeichenfolge "Standardbefehlswarteschlange", die in XML-Daten verwendet wird.

<span id="XML_D3D11_DEVICE_INFORMATION"></span><span id="xml_d3d11_device_information"></span>**XML \_ \_ D3D11-GERÄTEINFORMATIONEN \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "d3d11 device information".

<span id="XML_D3D12_DEVICE_INFORMATION"></span><span id="xml_d3d12_device_information"></span>**XML \_ \_ D3D12-GERÄTEINFORMATIONEN \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "d3d12 device information".

<span id="XML_CONSERVATIVE_RASTER"></span><span id="xml_conservative_raster"></span>**\_XML-KONSERVATIVES \_ RASTER**  
Ressourcen-ID für die Zeichenfolge "konservatives Raster", die in XML-Daten verwendet wird.

<span id="XML_INPUT_LAYOUT_12"></span><span id="xml_input_layout_12"></span>**\_ \_ XML-EINGABELAYOUT \_ 12**  
Ressourcen-ID für die Zeichenfolge "Eingabelayout 12", die in XML-Daten verwendet wird.

<span id="XML_GRAPHICS_PIPELINE_STATE_12"></span><span id="xml_graphics_pipeline_state_12"></span>**\_ \_ XML-GRAFIKPIPELINEZUSTAND \_ \_ 12**  
Ressourcen-ID für die Zeichenfolge "Grafikpipelinezustand 12", die in XML-Daten verwendet wird.

<span id="XML_BLEND_STATE_12"></span><span id="xml_blend_state_12"></span>**XML \_ BLEND \_ STATE \_ 12**  
Ressourcen-ID für die Zeichenfolge "blend state 12", die in XML-Daten verwendet wird.

<span id="XML_ROOT_SIGNATURE_12"></span><span id="xml_root_signature_12"></span>**XML \_ ROOT \_ SIGNATURE \_ 12**  
Ressourcen-ID für die Zeichenfolge "Stammsignatur 12", die in XML-Daten verwendet wird.

<span id="XML_STREAM_OUTPUT_12"></span><span id="xml_stream_output_12"></span>**XML \_ STREAM \_ OUTPUT \_ 12**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Streamausgabe 12".

<span id="XML_RASTERIZED_STREAM"></span><span id="xml_rasterized_stream"></span>**\_XML-RASTERDATENSTROM \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "rasterized stream".

<span id="XML_SO_DECLARATION_ENTRY_12"></span><span id="xml_so_declaration_entry_12"></span>**\_ \_ XML-SO-DEKLARATIONSEINTRAG \_ \_ 12**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "so declaration entry 12".

<span id="XML_START_COMPONENT"></span><span id="xml_start_component"></span>**XML \_ START \_ COMPONENT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "start component".

<span id="XML_COMPONENT_COUNT"></span><span id="xml_component_count"></span>**ANZAHL DER \_ \_ XML-KOMPONENTEN**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Komponentenanzahl".

<span id="XML_OUTPUT_SLOT"></span><span id="xml_output_slot"></span>**\_ \_ XML-AUSGABESLOT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Ausgabeslot".

<span id="XML_COMPUTE_PIPELINE_STATE_12"></span><span id="xml_compute_pipeline_state_12"></span>**XML \_ COMPUTE PIPELINE STATE \_ \_ \_ 12**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Computepipelinestatus 12".

<span id="XML_D3D12_SHADER_BYTECODE"></span><span id="xml_d3d12_shader_bytecode"></span>**XML \_ D3D12 \_ SHADER \_ BYTECODE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "d3d12 shader bytecode".

<span id="XML_D3D12_SHADER_BYTECODE_PTR"></span><span id="xml_d3d12_shader_bytecode_ptr"></span>**XML \_ D3D12 \_ SHADER \_ BYTECODE \_ PTR**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "d3d12 shader bytecode ptr".

<span id="XML_D3D12_SHADER_BYTECODE_LENGTH"></span><span id="xml_d3d12_shader_bytecode_length"></span>**XML \_ D3D12 \_ SHADER \_ BYTECODE \_ LENGTH**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "d3d12 shader bytecode length".

<span id="XML_D3D12_VIEW_DIMENSION"></span><span id="xml_d3d12_view_dimension"></span>**XML \_ D3D12 \_ VIEW \_ DIMENSION**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "d3d12 view dimension".

<span id="XML_D3D12_NOTHING_TO_DEFINE"></span><span id="xml_d3d12_nothing_to_define"></span>**XML \_ D3D12 \_ NOTHING \_ TO \_ DEFINE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "d3d12 nothing to define".

<span id="XML_OFFSET_IN_BYTES"></span><span id="xml_offset_in_bytes"></span>**\_XML-OFFSET \_ IN \_ BYTES**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "offset in bytes".

<span id="XML_SIZE_IN_BYTES"></span><span id="xml_size_in_bytes"></span>**\_ \_ XML-GRÖßE IN \_ BYTES**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "size in bytes".

<span id="XML_SO_CUR_OFFSET"></span><span id="xml_so_cur_offset"></span>**XML \_ SO \_ CUR \_ OFFSET**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "so cur offset".

<span id="XML_ROOT_SIGNATURE"></span><span id="xml_root_signature"></span>**\_ \_ XML-STAMMSIGNATUR**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammsignatur".

<span id="XML_NUM_PARAMETERS"></span><span id="xml_num_parameters"></span>**\_XML-NUM-PARAMETER \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "num parameters".

<span id="XML_ROOT_PARAMETERS"></span><span id="xml_root_parameters"></span>**\_ \_ XML-STAMMPARAMETER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "root parameters".

<span id="XML_ROOT_PARAMETER"></span><span id="xml_root_parameter"></span>**XML \_ ROOT \_ PARAMETER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "root parameter".

<span id="XML_ROOT_PARAMETER_TYPE"></span><span id="xml_root_parameter_type"></span>**XML \_ ROOT \_ PARAMETER \_ TYPE**  
Ressourcen-ID für die Zeichenfolge "Stammparametertyp", die in XML-Daten verwendet wird.

<span id="XML_ROOT_PARAMETER_SHADER_VISIBILITY"></span><span id="xml_root_parameter_shader_visibility"></span>**SICHTBARKEIT \_ DES \_ XML-STAMMPARAMETER-SHADERS \_ \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Sichtbarkeit des Shaders für Stammparameter".

<span id="XML_DESCRIPTOR_HANDLE"></span><span id="xml_descriptor_handle"></span>**\_XML-DESKRIPTORHAND \_ HANDLE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Deskriptorhand handle".

<span id="XML_DESCRIPTOR_HANDLE_PTR"></span><span id="xml_descriptor_handle_ptr"></span>**XML \_ DESCRIPTOR \_ HANDLE \_ PTR**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "descriptor handle ptr".

<span id="XML_ROOT_CONSTANTS"></span><span id="xml_root_constants"></span>**\_ \_ XML-STAMMKONST CONSTANTS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "root constants".

<span id="XML_ROOT_DESCRIPTOR"></span><span id="xml_root_descriptor"></span>**\_ \_ XML-STAMMDESKRIPTOR**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "root descriptor".

<span id="XML_ROOT_DESCRIPTOR_SHADER_REGISTER"></span><span id="xml_root_descriptor_shader_register"></span>**XML \_ ROOT \_ DESCRIPTOR \_ SHADER \_ REGISTER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "root descriptor shader register".

<span id="XML_ROOT_DESCRIPTOR_REGISTER_SPACE"></span><span id="xml_root_descriptor_register_space"></span>**XML \_ ROOT \_ DESCRIPTOR \_ REGISTER \_ SPACE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptorregisterbereich".

<span id="XML_ROOT_DESCRIPTOR_TABLE"></span><span id="xml_root_descriptor_table"></span>**\_ \_ XML-STAMMDESKRIPTORTABELLE \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptortabelle".

<span id="XML_ROOT_DESCRIPTOR_NUM_RANGES"></span><span id="xml_root_descriptor_num_ranges"></span>**XML \_ ROOT \_ DESCRIPTOR \_ NUM \_ RANGES**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "root descriptor num ranges".

<span id="XML_ROOT_DESCRIPTOR_RANGES"></span><span id="xml_root_descriptor_ranges"></span>**\_ \_ XML-STAMMDESKRIPTORBEREICHE \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptorbereiche".

<span id="XML_ROOT_DESCRIPTOR_RANGE"></span><span id="xml_root_descriptor_range"></span>**\_ \_ XML-STAMMDESKRIPTORBEREICH \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptorbereich".

<span id="XML_ROOT_DESCRIPTOR_RANGE_TYPE"></span><span id="xml_root_descriptor_range_type"></span>**\_ \_ XML-STAMMDESKRIPTORBEREICHSTYP \_ \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptorbereichstyp".

<span id="XML_ROOT_DESCRIPTOR_RANGE_COUNT"></span><span id="xml_root_descriptor_range_count"></span>**ANZAHL \_ VON \_ XML-STAMMDESKRIPTORBEREICH \_ \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptorbereichsanzahl".

<span id="XML_ROOT_DESCRIPTOR_RANGE_BASE_SHADER_REGISTER"></span><span id="xml_root_descriptor_range_base_shader_register"></span>**\_ \_ BASIS-SHADERREGISTER DES XML-STAMMDESKRIPTORBEREICHS \_ \_ \_ \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Basis-Shaderregister für Stammdeskriptorbereich".

<span id="XML_ROOT_DESCRIPTOR_RANGE_REGISTER_SPACE"></span><span id="xml_root_descriptor_range_register_space"></span>**XML \_ ROOT \_ DESCRIPTOR \_ RANGE \_ REGISTER \_ SPACE**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptorbereichsregisterbereich".

<span id="XML_ROOT_DESCRIPTOR_RANGE_OFFSET"></span><span id="xml_root_descriptor_range_offset"></span>**\_ \_ \_ XML-STAMMDESKRIPTORBEREICHSOFFSET \_**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Stammdeskriptorbereichsoffset".

<span id="XML_ROOT_CONSTANTS_SHADER_REGISTER"></span><span id="xml_root_constants_shader_register"></span>**\_SHADERREGISTER FÜR \_ XML-STAMMKONSTTEN \_ \_**  
Ressourcen-ID für die Zeichenfolge "Root Constants Shader Register", die in XML-Daten verwendet wird.

<span id="XML_ROOT_CONSTANTS_REGISTER_SPACE"></span><span id="xml_root_constants_register_space"></span>**XML \_ ROOT \_ CONSTANTS \_ REGISTER \_ SPACE**  
Ressourcen-ID für die Zeichenfolge "Stammkonst constants register space", die in XML-Daten verwendet wird.

<span id="XML_ROOT_CONSTANTS_INT32_COUNT"></span><span id="xml_root_constants_int32_count"></span>**XML \_ ROOT \_ CONSTANTS \_ INT32 \_ COUNT**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "root constants int32 count".

<span id="XML_NOT_SET"></span><span id="xml_not_set"></span>**XML \_ NOT \_ SET**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "not set".

<span id="XML_ERROR_RETRIEVING_VALUE"></span><span id="xml_error_retrieving_value"></span>**\_XML-FEHLER BEIM ABRUFEN DES \_ \_ WERTS**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Fehler beim Abrufen des Werts".

<span id="XML_VERTEX_SHADER"></span><span id="xml_vertex_shader"></span>**XML \_ VERTEX \_ SHADER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Vertex-Shader".

<span id="XML_PIXEL_SHADER"></span><span id="xml_pixel_shader"></span>**\_ \_ XML-PIXEL-SHADER**  
Ressourcen-ID für die in XML-Daten verwendete Zeichenfolge "Pixel-Shader".

<span id="XML_DOMAIN_SHADER"></span><span id="xml_domain_shader"></span>**XML \_ DOMAIN \_ SHADER**  
Ressourcen-ID für die Zeichenfolge "Domänen-Shader", die in XML-Daten verwendet wird.

<span id="XML_HULL_SHADER"></span><span id="xml_hull_shader"></span>**\_ \_ XML-HÜLLEN-SHADER**  
Ressourcen-ID für die Zeichenfolge "Hüllen-Shader", die in XML-Daten verwendet wird.

<span id="XML_GEOMETRY_SHADER"></span><span id="xml_geometry_shader"></span>**XML \_ GEOMETRY \_ SHADER**  
Ressourcen-ID für die Zeichenfolge "geometry shader", die in XML-Daten verwendet wird.

<span id="XML_ROOT_SIGNATURE_OBJECT"></span><span id="xml_root_signature_object"></span>**\_ \_ XML-STAMMSIGNATUROBJEKT \_**  
Ressourcen-ID für die Zeichenfolge "Root Signature Object", die in XML-Daten verwendet wird.

<span id="XML_ROOT_SIGNATURE_DESERIALIZED_DESC"></span><span id="xml_root_signature_deserialized_desc"></span>**\_ \_ DESERIALISIERTE XML-STAMMSIGNATUR \_ \_ DESC**  
Ressourcen-ID für die Zeichenfolge "root signature deserialized desc", die in XML-Daten verwendet wird.

<span id="XML_COMMAND_QUEUE"></span><span id="xml_command_queue"></span>**\_ \_ XML-BEFEHLSWARTESCHLANGE**  
Ressourcen-ID für die Zeichenfolge "Befehlswarteschlange", die in XML-Daten verwendet wird.

<span id="XML_D3D12_COMMAND_LIST_TYPE_TYPE"></span><span id="xml_d3d12_command_list_type_type"></span>**TYP DER XML \_ D3D12-BEFEHLSLISTE \_ \_ \_ \_**  
Ressourcen-ID für die Zeichenfolge "d3d12-Befehlslistentyp", die in XML-Daten verwendet wird.

<span id="XML_COMMAND_QUEUE_PRIORITY"></span><span id="xml_command_queue_priority"></span>**PRIORITÄT DER \_ XML-BEFEHLSWARTESCHLANGE \_ \_**  
Ressourcen-ID für die Zeichenfolge "Befehlswarteschlangenpriorität", die in XML-Daten verwendet wird.

<span id="XML_COMMAND_QUEUE_IS_DEFAULT"></span><span id="xml_command_queue_is_default"></span>**\_ \_ XML-BEFEHLSWARTESCHLANGE \_ IST \_ STANDARD**  
Ressourcen-ID für die Zeichenfolge "Befehlswarteschlange ist Standard" wird in XML-Daten verwendet.

<span id="XML_COMMAND_ALLOCATOR"></span><span id="xml_command_allocator"></span>**\_ \_ XML-BEFEHLSZUWEISUNG**  
Ressourcen-ID für die Zeichenfolge "Befehlszuweisung", die in XML-Daten verwendet wird.

<span id="XML_D3D12_RESOURCE"></span><span id="xml_d3d12_resource"></span>**XML \_ D3D12-RESSOURCE \_**  
Ressourcen-ID für die Zeichenfolge "d3d12 resource", die in XML-Daten verwendet wird.

<span id="XML_D3D12_DESCRIPTOR_HEAP"></span><span id="xml_d3d12_descriptor_heap"></span>**XML \_ D3D12 \_ DESCRIPTOR \_ HEAP**  
Ressourcen-ID für die Zeichenfolge "d3d12 descriptor heap", die in XML-Daten verwendet wird.

<span id="XML_D3D12_DESCRIPTOR"></span><span id="xml_d3d12_descriptor"></span>**XML \_ D3D12 \_ DESCRIPTOR**  
Ressourcen-ID für die Zeichenfolge "d3d12 descriptor", die in XML-Daten verwendet wird.

<span id="XML_D3D12_RTV"></span><span id="xml_d3d12_rtv"></span>**XML \_ D3D12 \_ RTV**  
Ressourcen-ID für die Zeichenfolge "d3d12 rtv", die in XML-Daten verwendet wird.

<span id="XML_D3D12_VBV"></span><span id="xml_d3d12_vbv"></span>**XML \_ D3D12 \_ VBV**  
Ressourcen-ID für die Zeichenfolge "d3d12 vbv", die in XML-Daten verwendet wird.

<span id="XML_D3D12_IBV"></span><span id="xml_d3d12_ibv"></span>**XML \_ D3D12 \_ IBV**  
Ressourcen-ID für die Zeichenfolge "d3d12 ibv", die in XML-Daten verwendet wird.

<span id="XML_D3D12_CBV"></span><span id="xml_d3d12_cbv"></span>**XML \_ D3D12 \_ CBV**  
Ressourcen-ID für die Zeichenfolge "d3d12 cbv", die in XML-Daten verwendet wird.

<span id="XML_D3D12_SRV"></span><span id="xml_d3d12_srv"></span>**XML \_ D3D12 \_ SRV**  
Ressourcen-ID für die Zeichenfolge "d3d12 srv", die in XML-Daten verwendet wird.

<span id="XML_D3D12_UAV"></span><span id="xml_d3d12_uav"></span>**XML \_ D3D12 \_ UAV**  
Ressourcen-ID für die Zeichenfolge "d3d12 uav", die in XML-Daten verwendet wird.

<span id="XML_D3D12_SOV"></span><span id="xml_d3d12_sov"></span>**XML \_ D3D12 \_ SOV**  
Ressourcen-ID für die Zeichenfolge "d3d12 sov", die in XML-Daten verwendet wird.

<span id="XML_D3D12_SAMPLER"></span><span id="xml_d3d12_sampler"></span>**XML \_ D3D12 \_ SAMPLER**  
Ressourcen-ID für die Zeichenfolge "d3d12 sampler", die in XML-Daten verwendet wird.

<span id="XML_D3D12_DSV"></span><span id="xml_d3d12_dsv"></span>**XML \_ D3D12 \_ DSV**  
Ressourcen-ID für die Zeichenfolge "d3d12 dsv", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_1D"></span><span id="xml_texture_1d"></span>**XML \_ TEXTURE \_ 1D**  
Ressourcen-ID für die Zeichenfolge "texture 1d", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_1DARRAY"></span><span id="xml_texture_1darray"></span>**\_XML-TEXTUR \_ 1DARRAY**  
Ressourcen-ID für die Zeichenfolge "texture 1darray", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_2D"></span><span id="xml_texture_2d"></span>**XML \_ TEXTURE \_ 2D**  
Ressourcen-ID für die zeichenfolge "texture 2d", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_2DARRAY"></span><span id="xml_texture_2darray"></span>**XML \_ TEXTURE \_ 2DARRAY**  
Ressourcen-ID für die Zeichenfolge "texture 2darray", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_2DMS"></span><span id="xml_texture_2dms"></span>**XML \_ TEXTURE \_ 2DMS**  
Ressourcen-ID für die Zeichenfolge "texture 2dms", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_2DMSARRAY"></span><span id="xml_texture_2dmsarray"></span>**XML \_ TEXTURE \_ 2DMSARRAY**  
Ressourcen-ID für die Zeichenfolge "texture 2dmsarray", die in XML-Daten verwendet wird.

<span id="XML_TEXTURE_3D"></span><span id="xml_texture_3d"></span>**XML \_ TEXTURE \_ 3D**  
Ressourcen-ID für die Zeichenfolge "Texture 3d", die in XML-Daten verwendet wird.

<span id="XML_MISCELLANEOUS"></span><span id="xml_miscellaneous"></span>**XML \_ MISCELLANEOUS**  
Ressourcen-ID für die Zeichenfolge "sonstiges", das in XML-Daten verwendet wird.

<span id="XML_RENDER_TARGET_VIEW_12"></span><span id="xml_render_target_view_12"></span>**\_ \_ XML-RENDERZIELANSICHT \_ \_ 12**  
Ressourcen-ID für die Zeichenfolge "Renderzielansicht 12", die in XML-Daten verwendet wird.

<span id="XML_STRIDE_COUNT"></span><span id="xml_stride_count"></span>**XML \_ STRIDE \_ COUNT**  
Ressourcen-ID für die Zeichenfolge "stride count", die in XML-Daten verwendet wird.

<span id="XML_SHADER_ROOT_SIGNATURES"></span><span id="xml_shader_root_signatures"></span>**\_ \_ XML-SHADER-STAMMSIGNATUREN \_**  
Ressourcen-ID für die Zeichenfolge "Shaderstammsignaturen", die in XML-Daten verwendet wird.

<span id="XML_CMDLIST_ROOT_SIGNATURE_OBJECT"></span><span id="xml_cmdlist_root_signature_object"></span>**XML \_ \_ CMDLIST-STAMMSIGNATUROBJEKT \_ \_**  
Ressourcen-ID für die Zeichenfolge "cmdlist root signature object", die in XML-Daten verwendet wird.

<span id="XML_INPUT_LAYOUT_ELEMENT_COUNT"></span><span id="xml_input_layout_element_count"></span>**\_ANZAHL VON XML-EINGABELAYOUTELEMENTEN \_ \_ \_**  
Ressourcen-ID für die Zeichenfolge "Anzahl von Eingabelayoutelementen", die in XML-Daten verwendet wird.

<span id="XML_INPUT_LAYOUT_ELEMENT_DESCRIPTIONS"></span><span id="xml_input_layout_element_descriptions"></span>**BESCHREIBUNGEN VON \_ XML-EINGABELAYOUTELEMENTEN \_ \_ \_**  
Ressourcen-ID für die Zeichenfolge "Eingabelayoutelementbeschreibungen", die in XML-Daten verwendet wird.

<span id="XML_SHADER_DESCRIPTION"></span><span id="xml_shader_description"></span>**\_ \_ XML-SHADERBESCHREIBUNG**  
Ressourcen-ID für die Zeichenfolge "shader description", die in XML-Daten verwendet wird.

<span id="XML_PSO_ROOT_SIGNATURE_OBJECT"></span><span id="xml_pso_root_signature_object"></span>**\_ \_ XML-PSO-STAMMSIGNATUROBJEKT \_ \_**  
Ressourcen-ID für die Zeichenfolge "pso root signature object", die in XML-Daten verwendet wird.

<span id="XML_BUFFER_LOCATION"></span><span id="xml_buffer_location"></span>**SPEICHERORT DES \_ XML-PUFFERS \_**  
Ressourcen-ID für die Zeichenfolge "Pufferspeicherort", die in XML-Daten verwendet wird.

<span id="XML_BUFFER_FILLED_LOCATION"></span><span id="xml_buffer_filled_location"></span>**SPEICHERORT DES \_ \_ XML-PUFFERS \_**  
Ressourcen-ID für die Zeichenfolge "Puffer gefüllter Speicherort", die in XML-Daten verwendet wird.

<span id="XML_D3D12_VIRTUAL_ADDRESS"></span><span id="xml_d3d12_virtual_address"></span>**VIRTUELLE XML \_ D3D12-ADRESSE \_ \_**  
Ressourcen-ID für die Zeichenfolge "d3d12 virtual address", die in XML-Daten verwendet wird.

<span id="XML_D3D12_VIRTUAL_ADDRESS_OFFSET"></span><span id="xml_d3d12_virtual_address_offset"></span>**XML \_ D3D12 \_ VIRTUAL \_ ADDRESS \_ OFFSET**  
Ressourcen-ID für die Zeichenfolge "d3d12 virtual address offset", die in XML-Daten verwendet wird.

<span id="XML_D3D12_OFFSET_IN_BYTES"></span><span id="xml_d3d12_offset_in_bytes"></span>**XML \_ D3D12 \_ OFFSET \_ IN \_ BYTES**  
Ressourcen-ID für die Zeichenfolge "d3d12 offset in bytes", die in XML-Daten verwendet wird.

<span id="XML_MAPPED_TILES"></span><span id="xml_mapped_tiles"></span>**\_ZUGEORDNETE \_ XML-KACHELN**  
Ressourcen-ID für die Zeichenfolge "zugeordnete Kacheln", die in XML-Daten verwendet wird.

<span id="XML_POOL_TILES"></span><span id="xml_pool_tiles"></span>**\_ \_ XML-POOLKACHELN**  
Ressourcen-ID für die Zeichenfolge "Poolkacheln", die in XML-Daten verwendet wird.

<span id="XML_TILE"></span><span id="xml_tile"></span>**\_XML-KACHEL**  
Ressourcen-ID für die zeichenfolge "tile", die in XML-Daten verwendet wird.

<span id="XML_OPTIONS"></span><span id="xml_options"></span>**\_XML-OPTIONEN**  
Ressourcen-ID für die Zeichenfolge "Optionen", die in XML-Daten verwendet wird.

<span id="XML_TILE_LIST_MODE"></span><span id="xml_tile_list_mode"></span>**\_ \_ \_ XML-KACHELLISTENMODUS**  
Ressourcen-ID für die Zeichenfolge "Kachellistenmodus", die in XML-Daten verwendet wird.

<span id="XML_TILE_POOL_SLOT"></span><span id="xml_tile_pool_slot"></span>**\_ \_ \_ XML-KACHELPOOLSLOT**  
Ressourcen-ID für die Zeichenfolge "Kachelpoolslot", die in XML-Daten verwendet wird.

<span id="XML_TILE_COORDINATE"></span><span id="xml_tile_coordinate"></span>**\_ \_ XML-KACHELKOORDINATE**  
Ressourcen-ID für die Zeichenfolge "Kachelkoordinate", die in XML-Daten verwendet wird.

<span id="XML_HEAP_TYPE"></span><span id="xml_heap_type"></span>**\_ \_ XML-HEAPTYP**  
Ressourcen-ID für die Zeichenfolge "Heaptyp", die in XML-Daten verwendet wird.

<span id="XML_RESOURCE_USAGE"></span><span id="xml_resource_usage"></span>**VERWENDUNG \_ VON XML-RESSOURCEN \_**  
Ressourcen-ID für die Zeichenfolge "Ressourcenverwendung", die in XML-Daten verwendet wird.

<span id="XML_RESOURCE_USAGES"></span><span id="xml_resource_usages"></span>**VERWENDUNG \_ VON XML-RESSOURCEN \_**  
Ressourcen-ID für die Zeichenfolge "Ressourcenverwendungen", die in XML-Daten verwendet wird.

<span id="XML_SUBRESOURCE"></span><span id="xml_subresource"></span>**XML \_ SUBRESOURCE**  
Ressourcen-ID für die Zeichenfolge "subresource", die in XML-Daten verwendet wird.

<span id="XML_SUBRESOURCE_USAGE"></span><span id="xml_subresource_usage"></span>**VERWENDUNG DER \_ XML-UNTERRESSOURCE \_**  
Ressourcen-ID für die Zeichenfolge "Subressourcenverwendung", die in XML-Daten verwendet wird.

<span id="XML_COMPUTE_SHADER"></span><span id="xml_compute_shader"></span>**\_ \_ XML-COMPUTE-SHADER**  
Ressourcen-ID für die Zeichenfolge "Compute-Shader", die in XML-Daten verwendet wird.

<span id="XML_COMMAND_LIST_12"></span><span id="xml_command_list_12"></span>**\_ \_ XML-BEFEHLSLISTE \_ 12**  
Ressourcen-ID für die Zeichenfolge "Befehlsliste 12", die in XML-Daten verwendet wird.

<span id="XML_PSO_RENDER_TARGET_FORMATS"></span><span id="xml_pso_render_target_formats"></span>**XML \_ \_ PSO-RENDERZIELFORMATE \_ \_**  
Ressourcen-ID für die Zeichenfolge "PSO-Renderzielformate", die in XML-Daten verwendet wird.

<span id="XML_RENDER_TARGET_VIEWS"></span><span id="xml_render_target_views"></span>**\_ \_ \_ XML-RENDERZIELSICHTEN**  
Ressourcen-ID für die Zeichenfolge "Renderzielansichten", die in XML-Daten verwendet wird.

<span id="XML_SHADER_RESOURCE_VIEWS"></span><span id="xml_shader_resource_views"></span>**\_ \_ XML-SHADERRESSOURCENANSICHTEN \_**  
Ressourcen-ID für die Zeichenfolge "Shaderressourcenansichten", die in XML-Daten verwendet wird.

## <a name="requirements"></a>Requirements (Anforderungen)

<table><colgroup><col  /><col  /></colgroup><tbody><tr class="odd"><td><p>Header</p></td><td>Vspixengine.h</td></tr></tbody></table>

 

 



