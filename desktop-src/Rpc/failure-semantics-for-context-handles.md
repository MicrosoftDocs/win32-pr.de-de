---
title: Fehler Semantik für Kontext Handles
description: In diesem Thema wird die Fehler Semantik für Kontext Handles erläutert.
ms.assetid: fcf28519-39ad-4823-bc27-f3502e4d540c
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: c4528b3f5160b92a4e6f10dbcf877e9fec59f81b
ms.sourcegitcommit: 2d531328b6ed82d4ad971a45a5131b430c5866f7
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 09/16/2019
ms.locfileid: "104037025"
---
# <a name="failure-semantics-for-context-handles"></a><span data-ttu-id="79eea-103">Fehler Semantik für Kontext Handles</span><span class="sxs-lookup"><span data-stu-id="79eea-103">Failure Semantics for Context Handles</span></span>

<span data-ttu-id="79eea-104">In diesem Thema wird die Fehler Semantik für Kontext Handles erläutert.</span><span class="sxs-lookup"><span data-stu-id="79eea-104">This topic discusses failure semantics for context handles.</span></span>

## <a name="failure-semantics-when-closing-the-context-handle-fails"></a><span data-ttu-id="79eea-105">Fehler Semantik beim Schließen des Kontext Handles schlägt fehl</span><span class="sxs-lookup"><span data-stu-id="79eea-105">Failure Semantics when Closing the Context Handle Fails</span></span>

<span data-ttu-id="79eea-106">Stellen Sie sich vor, eine Client Anwendung versucht, ein auf dem Server geöffnetes Kontext Handle zu schließen, ohne den Client Prozess zu schließen.</span><span class="sxs-lookup"><span data-stu-id="79eea-106">Imagine a client application is attempting to close a context handle it opened on the server, without shutting down the client process.</span></span> <span data-ttu-id="79eea-107">Gehen Sie außerdem davon aus, dass beim Server zum Schließen des Kontext Handles ein Fehler auftritt (z. b. wenn der Client nicht über genügend Arbeitsspeicher verfügt).</span><span class="sxs-lookup"><span data-stu-id="79eea-107">Also, assume the call to the server to close the context handle fails (for example, the client is out of memory).</span></span> <span data-ttu-id="79eea-108">Die richtige Vorgehensweise zum Behandeln dieser Situation besteht darin, die [**rpcssdestroyclientcontext**](/windows/desktop/api/Rpcndr/nf-rpcndr-rpcssdestroyclientcontext) -Funktion aufzurufen.</span><span class="sxs-lookup"><span data-stu-id="79eea-108">The proper way to handle this situation is to call the [**RpcSsDestroyClientContext**](/windows/desktop/api/Rpcndr/nf-rpcndr-rpcssdestroyclientcontext) function.</span></span> <span data-ttu-id="79eea-109">In einem solchen Fall bereinigt der Client seine Seite des Kontext Handles und schließt die Verbindung mit dem Server abgebrochen.</span><span class="sxs-lookup"><span data-stu-id="79eea-109">In such a case the client cleans up its side of the context handle, and abortively closes the connection to the server.</span></span> <span data-ttu-id="79eea-110">Da es sich bei der Verbindung tatsächlich um einen Verbindungspool handelt (siehe [RPC und das Netzwerk](rpc-and-the-network.md)), bei dem es sich um eine Verweis Zählung mit einem Verweis für jede geöffnete Bindung oder ein geöffnetes Kontext Handle handelt, wird die Verbindung durch den Aufruf der **rpcssdestroyclientcontext** -Funktion nicht zerstört.</span><span class="sxs-lookup"><span data-stu-id="79eea-110">Since the connection is really a connection pool (see [RPC and the Network](rpc-and-the-network.md)), which is reference-counted with one reference for each opened binding or context handle, destroying the context handle by calling the **RpcSsDestroyClientContext** function does not actually destroy the connection.</span></span> <span data-ttu-id="79eea-111">Stattdessen wird der Verweis Zähler für den Verbindungspool verringert.</span><span class="sxs-lookup"><span data-stu-id="79eea-111">Rather, it decrements the reference count for the connection pool.</span></span> <span data-ttu-id="79eea-112">Damit Verbindungen im Pool geschlossen werden können, muss der Client alle Bindungs Handles und Kontext Handles für diesen Server aus dem Client Prozess schließen.</span><span class="sxs-lookup"><span data-stu-id="79eea-112">For connections in the pool to be closed, the client needs to close all binding handles and context handles to that server from the client process.</span></span> <span data-ttu-id="79eea-113">Anschließend werden alle Verbindungen im Pool geschlossen, und der Server-Lauf zeitmechanismus wird initiiert und bereinigt.</span><span class="sxs-lookup"><span data-stu-id="79eea-113">Then all connections in the pool are closed, and the server run-down mechanism is initiated and cleans up.</span></span>

## <a name="failure-semantics-during-change-of-state-of-the-context-handle"></a><span data-ttu-id="79eea-114">Fehler Semantik beim Ändern des Zustands des Kontext Handles</span><span class="sxs-lookup"><span data-stu-id="79eea-114">Failure Semantics During Change of State of the Context Handle</span></span>

<span data-ttu-id="79eea-115">Die Informationen in diesem Abschnitt beziehen sich auf Windows XP-und spätere Plattformen.</span><span class="sxs-lookup"><span data-stu-id="79eea-115">The information in this section refers to Windows XP and later platforms.</span></span>

<span data-ttu-id="79eea-116">Kontext Handles sind einfach Parameter für eine Funktion.</span><span class="sxs-lookup"><span data-stu-id="79eea-116">Context handles are simply parameters to a function.</span></span> <span data-ttu-id="79eea-117">Alle Änderungen im Zustand eines Kontext Handles treten auf, wenn Parameter gemarshallt oder nicht gemarshallt werden.</span><span class="sxs-lookup"><span data-stu-id="79eea-117">All changes in the state of a context handle happen when parameters are marshaled or unmarshaled.</span></span> <span data-ttu-id="79eea-118">Wenn ein Client z. b. ein Kontext Handle öffnet (von **null** in einen nicht-**null**-Wert geändert wird), öffnet die RPC-Laufzeit den RPC-Teil des Handles nicht, bis die Argumente für das Senden an den Client gemarshallt werden.</span><span class="sxs-lookup"><span data-stu-id="79eea-118">For example, if a client opens a context handle (changes it from **NULL** to non-**NULL**), the RPC run-time does not actually open the RPC portion of the handle until the arguments are marshaled for sending to the client.</span></span> <span data-ttu-id="79eea-119">Während der Zwischenzeit können Fehler auftreten.</span><span class="sxs-lookup"><span data-stu-id="79eea-119">Failures can occur during the interim.</span></span> <span data-ttu-id="79eea-120">Aufgrund einer Vielzahl möglicher Netzwerk-oder geringer Ressourcen Bedingungen kann die Übertragung des Pakets an den Client fehlschlagen.</span><span class="sxs-lookup"><span data-stu-id="79eea-120">Due to a variety of possible network or low resource conditions, transmission of the packet to the client could fail.</span></span> <span data-ttu-id="79eea-121">Oder die Server Routine löst eine Ausnahme aus, wenn versucht wird, ein Kontext Handle zu ändern.</span><span class="sxs-lookup"><span data-stu-id="79eea-121">Or the server routine may throw an exception while attempting to change a context handle.</span></span> <span data-ttu-id="79eea-122">In diesen oder anderen Fehlersituationen erhalten der Client und der Server möglicherweise inkonsistente Sichten des Kontext Handles.</span><span class="sxs-lookup"><span data-stu-id="79eea-122">In these or other failure situations, the client and server may get inconsistent views of the context handle.</span></span> <span data-ttu-id="79eea-123">In diesem Abschnitt wird die Regel für den Status des Kontext Handles und die Verantwortung des Client-und Server Codes bei verschiedenen Fehlerbedingungen erläutert.</span><span class="sxs-lookup"><span data-stu-id="79eea-123">This section explains the rule for the state of the context handle, and the responsibility of client and server code during various failure conditions.</span></span>

-   <span data-ttu-id="79eea-124">Ein **null** -Kontext Handle kommt an, aber die Server Routine stößt auf einen Fehler und löst eine Ausnahme aus.</span><span class="sxs-lookup"><span data-stu-id="79eea-124">A **NULL** context handle arrives, but the server routine encounters a failure and throws an exception.</span></span>

    <span data-ttu-id="79eea-125">Es liegt in der Verantwortung der Server Routine, alle Kontext Handles – den Zustand, den Sie möglicherweise erstellt hat, zu bereinigen.</span><span class="sxs-lookup"><span data-stu-id="79eea-125">It is the responsibility of the server routine to clean up any context handle–related state it may have created.</span></span> <span data-ttu-id="79eea-126">Der Status der RPC-Laufzeit wird bereinigt.</span><span class="sxs-lookup"><span data-stu-id="79eea-126">The RPC run time cleans up its state.</span></span>

-   <span data-ttu-id="79eea-127">Nicht-**null** -Kontext Handles werden empfangen, aber die Server Routine stößt auf einen Fehler und löst eine Ausnahme aus.</span><span class="sxs-lookup"><span data-stu-id="79eea-127">A non-**NULL** context handles arrives, but the server routine encounters a failure and throws an exception.</span></span>

    <span data-ttu-id="79eea-128">Wenn die Server Routine das Kontext Handle geschlossen hat, weiß der Client es nicht, da der-Vorgang nicht erfolgreich ist. die weitere Verwendung des Kontext Handles führt zu einem RPC- \_ X- \_ SS-Kontext Konflikt \_ \_ Fehler auf dem Client.</span><span class="sxs-lookup"><span data-stu-id="79eea-128">If the server routine closed the context handle, the client will not know about it, since the call will not succeed; further use of the context handle will result in an RPC\_X\_SS\_CONTEXT\_MISMATCH error on the client.</span></span> <span data-ttu-id="79eea-129">Wenn die Server Routine das Kontext Handle nicht ändert, kann Sie vom Client weiterhin verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="79eea-129">If the server routine does not modify the context handle, the client can still use it.</span></span> <span data-ttu-id="79eea-130">Wenn die Server Routine die im Server Kontext gespeicherten Informationen ändert, werden diese Informationen von neuen Aufrufen des Clients verwendet.</span><span class="sxs-lookup"><span data-stu-id="79eea-130">If the server routine changes the information stored in the server context, new calls from the client will use that information.</span></span>

-   <span data-ttu-id="79eea-131">Ein Kontext Handle, das nicht **null** ist, wird erreicht, und die Server Routine schließt den Handle, aber entweder nach dem Marshalling des Kontext Handles oder bei der Verarbeitung nach dem Mars Hallen ist ein Fehler aufgetreten.</span><span class="sxs-lookup"><span data-stu-id="79eea-131">A non-**NULL** context handle arrives, and the server routine closes the handle, but either marshaling after the context handle was marshaled failed, or processing after marshaling failed.</span></span>

    <span data-ttu-id="79eea-132">Das Kontext Handle ist geschlossen, und weitere Aufrufe dieses Clients mit diesem Kontext Handle führen zu einem Fehler im RPC \_ X \_ SS-Kontext Konflikt \_ \_ auf dem Client.</span><span class="sxs-lookup"><span data-stu-id="79eea-132">The context handle is closed, and further calls by this client using this context handle result in an RPC\_X\_SS\_CONTEXT\_MISMATCH error on the client.</span></span>

-   <span data-ttu-id="79eea-133">Ein **null** -Kontext Handle wird erreicht, und der Server erstellt seinen Kontext für dieses Handle, aber entweder nach dem Marshalling des Kontext Handles oder nach dem Fehler bei der Verarbeitung nach dem Mars Hallen.</span><span class="sxs-lookup"><span data-stu-id="79eea-133">A **NULL** context handle arrives, and the server creates its context for this handle, but either marshaling after the context handle was marshaled failed, or processing after marshaling failed.</span></span>

    <span data-ttu-id="79eea-134">In diesem Fall ruft die RPC-Laufzeit den Testlauf für dieses Kontext Handle auf und bereinigt den RPC-Status für dieses Kontext handle.</span><span class="sxs-lookup"><span data-stu-id="79eea-134">In this case, the RPC run time invokes the run down for this context handle, and cleans up the RPC state for this context handle.</span></span> <span data-ttu-id="79eea-135">Das Kontext Handle wird nicht auf der Clientseite erstellt.</span><span class="sxs-lookup"><span data-stu-id="79eea-135">The context handle will not be created on the client side.</span></span>

-   <span data-ttu-id="79eea-136">Ein Kontext Handle, das nicht **null** ist, wird erreicht, und der Server ändert weder das Kontext Handle noch die im Server Kontext gespeicherten Informationen, und das Marshalling schlägt fehl, nachdem das Kontext Handle gemarshallt wurde.</span><span class="sxs-lookup"><span data-stu-id="79eea-136">A non-**NULL** context handle arrives, and the server either does not change the context handle, or it changes the information stored in the server context, and marshaling fails after the context handle is marshaled.</span></span>

    <span data-ttu-id="79eea-137">Neue Aufrufe vom Client verwenden das Kontext Handle, das der Server hat.</span><span class="sxs-lookup"><span data-stu-id="79eea-137">New calls from the client will use the context handle that the server has.</span></span>

-   <span data-ttu-id="79eea-138">Ein **null** -Kontext Handle wird erreicht, und der Server legt ihn nicht auf einen anderen Wert als **null** fest, aber der-Rückruf schlägt fehl, bevor das Kontext Handle gemarshallt wird.</span><span class="sxs-lookup"><span data-stu-id="79eea-138">A **NULL** context handle arrives, and the server does not set it to anything other than **NULL**, but the call fails before the context handle is marshaled.</span></span>

    <span data-ttu-id="79eea-139">In diesem Fall wird kein Kontext Handle auf dem Client erstellt.</span><span class="sxs-lookup"><span data-stu-id="79eea-139">In this case, no context handle is created on the client.</span></span>

-   <span data-ttu-id="79eea-140">Ein Kontext Handle, das nicht **null** ist, wird erreicht, und der Server legt den Wert auf **null** fest, aber das Marshalling schlägt fehl, bevor das Kontext Handle gemarshallt wird.</span><span class="sxs-lookup"><span data-stu-id="79eea-140">A non-**NULL** context handle arrives, and the server sets it to **NULL**, but marshaling fails before the context handle is marshaled.</span></span>

    <span data-ttu-id="79eea-141">In diesem Fall bleibt das Kontext Handle auf dem Server geschlossen, und der Client erhält \_ \_ \_ \_ beim Versuch, das Kontext Handle zu verwenden, nicht übereinstimmende RPC X SS-Kontext Fehler.</span><span class="sxs-lookup"><span data-stu-id="79eea-141">In this case, the context handle remains closed on the server, and the client gets RPC\_X\_SS\_CONTEXT\_MISMATCH errors when it tries to use the context handle.</span></span>

-   <span data-ttu-id="79eea-142">Ein **null** -Kontext Handle kommt auf dem Server an, und der Server legt ihn auf einen nicht-**null**-Wert fest, aber das Marshalling schlägt fehl, bevor das Kontext Handle gemarshallt wird.</span><span class="sxs-lookup"><span data-stu-id="79eea-142">A **NULL** context handle arrives on the server, and the server sets it to non-**NULL**, but marshaling fails before the context handle is marshaled.</span></span>

    <span data-ttu-id="79eea-143">Das Kontext Handle wird aufgerufen, sodass der Server bereinigt werden kann, und auf dem Client wird kein Kontext Handle erstellt.</span><span class="sxs-lookup"><span data-stu-id="79eea-143">The context handle run down is to be invoked so that the server can clean up, and no context handle will be created on the client.</span></span>

-   <span data-ttu-id="79eea-144">Ein Kontext Handle, das nicht **null** ist, wird erreicht, und der Server ändert weder das Kontext Handle noch die im Server Kontext gespeicherten Informationen, und das Marshalling schlägt fehl, bevor das Kontext Handle gemarshallt wird.</span><span class="sxs-lookup"><span data-stu-id="79eea-144">A non-**NULL** context handle arrives, and the server either does not change the context handle, or it changes the information stored in the server context, and marshaling fails before the context handle is marshaled.</span></span>

    <span data-ttu-id="79eea-145">Neue Aufrufe vom Client verwenden den Status auf dem Server.</span><span class="sxs-lookup"><span data-stu-id="79eea-145">New calls from the client will use the state on the server.</span></span>

-   <span data-ttu-id="79eea-146">Ein Kontext Handle wird als Rückgabewert deklariert, und die Server Routine gibt für das Kontext Handle **null** zurück, und das Marshalling schlägt fehl, bevor das Kontext Handle gemarshallt wird.</span><span class="sxs-lookup"><span data-stu-id="79eea-146">A context handle is declared as a return value, and the server routine returns **NULL** for the context handle and marshaling fails before the context handle is marshaled.</span></span>

    <span data-ttu-id="79eea-147">In diesem Fall wird kein neuer Kontext auf dem Client erstellt.</span><span class="sxs-lookup"><span data-stu-id="79eea-147">In this case, no new context is created on the client.</span></span>

-   <span data-ttu-id="79eea-148">Ein Kontext Handle wird als Rückgabewert deklariert, und die Server Routine gibt für das Kontext Handle einen Wert zurück, der nicht **null** ist, und das Marshalling schlägt fehl, bevor das Kontext Handle gemarshallt wird.</span><span class="sxs-lookup"><span data-stu-id="79eea-148">A context handle is declared as a return value, and the server routine returns non-**NULL** for the context handle and marshaling fails before the context handle is marshaled.</span></span>

    <span data-ttu-id="79eea-149">Die RPC-Laufzeit ruft die-Kontext Handle-Lauf Zeit Routine auf, um eine Bereinigung zu erhalten, und auf dem Client wird kein neuer Kontext erstellt.</span><span class="sxs-lookup"><span data-stu-id="79eea-149">The RPC run time calls the context handle run-down routine to give it a chance to clean up, and no new context is created on the client.</span></span>

 

 




