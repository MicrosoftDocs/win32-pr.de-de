---
description: 'Das Festlegen des FVF-Parameters der IDirect3DDevice9:: up-Methode auf einen Wert ungleich 0 (null) gibt an, dass der Pufferinhalt durch einen FVF-Code gekennzeichnet werden soll.'
ms.assetid: 7cab559f-3e9d-46bd-b00f-439e0922aeeb
title: Vertex-Puffer von "f" (Direct3D 9)
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: a86201c3ddc1cab6d492539caccc61c1430b3a2c
ms.sourcegitcommit: a47bd86f517de76374e4fff33cfeb613eb259a7e
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 01/06/2021
ms.locfileid: "106341094"
---
# <a name="fvf-vertex-buffers-direct3d-9"></a><span data-ttu-id="dc164-103">Vertex-Puffer von "f" (Direct3D 9)</span><span class="sxs-lookup"><span data-stu-id="dc164-103">FVF Vertex Buffers (Direct3D 9)</span></span>

<span data-ttu-id="dc164-104">Das Festlegen des FVF-Parameters der [**IDirect3DDevice9::**](/windows/desktop/api) up-Methode auf einen Wert ungleich 0 (null) gibt an, dass der Pufferinhalt durch einen FVF-Code gekennzeichnet werden soll.</span><span class="sxs-lookup"><span data-stu-id="dc164-104">Setting the FVF parameter of the [**IDirect3DDevice9::CreateVertexBuffer**](/windows/desktop/api) method to a nonzero value, which must be a valid FVF code, indicates that the buffer content is to be characterized by an FVF code.</span></span> <span data-ttu-id="dc164-105">Ein Vertex-Puffer, der mit einem FVF-Code erstellt wird, wird als FVF-Vertex-Puffer bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="dc164-105">A vertex buffer that is created with an FVF code is referred to as an FVF vertex buffer.</span></span> <span data-ttu-id="dc164-106">Einige Methoden oder Verwendungsmöglichkeiten von [**IDirect3DDevice9**](/windows/desktop/api) erfordern die Verwendung von b-Vertex-Puffern, andere erfordern nicht-b-b-Vertex-Puffer.</span><span class="sxs-lookup"><span data-stu-id="dc164-106">Some methods or uses of [**IDirect3DDevice9**](/windows/desktop/api) require FVF vertex buffers, and others require non-FVF vertex buffers.</span></span> <span data-ttu-id="dc164-107">Als Ziel-Vertex-Puffer Argument für [**IDirect3DDevice9::P rozess Vertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices)sind BTEX-Puffer Puffer erforderlich.</span><span class="sxs-lookup"><span data-stu-id="dc164-107">FVF vertex buffers are required as the destination vertex buffer argument for [**IDirect3DDevice9::ProcessVertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices).</span></span>

<span data-ttu-id="dc164-108">FVF-Vertex-Puffer können an einen Quelldaten Strom für beliebige Datenstrom Nummern gebunden werden.</span><span class="sxs-lookup"><span data-stu-id="dc164-108">FVF vertex buffers can be bound to a source data stream for any stream number.</span></span>

<span data-ttu-id="dc164-109">Das vorhanden sein der D3DFVF \_ xyzrhw-Komponente auf FVF-Vertexpuffern gibt an, dass die Scheitel Punkte in diesem Puffer verarbeitet wurden.</span><span class="sxs-lookup"><span data-stu-id="dc164-109">The presence of the D3DFVF\_XYZRHW component on FVF vertex buffers indicates that the vertices in that buffer have been processed.</span></span> <span data-ttu-id="dc164-110">Für [**IDirect3DDevice9::P rozess Vertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices) verwendete Scheitelpunkt Puffer müssen nach der Verarbeitung verarbeitet werden.</span><span class="sxs-lookup"><span data-stu-id="dc164-110">Vertex buffers used for [**IDirect3DDevice9::ProcessVertices**](/windows/win32/api/d3d9helper/nf-d3d9helper-idirect3ddevice9-processvertices) destination vertex buffers must be post-processed.</span></span> <span data-ttu-id="dc164-111">Vertex-Puffer, die für die Shader-Eingaben fester Funktionen verwendet werden, können entweder vorverarbeitet oder postverarbeitet werden.</span><span class="sxs-lookup"><span data-stu-id="dc164-111">Vertex buffers used for fixed function shader inputs can be either preprocessed or postprocessed.</span></span> <span data-ttu-id="dc164-112">Wenn der Vertex-Puffer nachträglich verarbeitet wird, wird der Shader effektiv umgangen, und die Daten werden direkt an das primitive Clipping-und Setup-Modul übergeben.</span><span class="sxs-lookup"><span data-stu-id="dc164-112">If the vertex buffer is post-processed, then the shader is effectively bypassed and the data is passed directly to the primitive clipping and setup module.</span></span>

<span data-ttu-id="dc164-113">Die Vertex-Puffer von "f" können mit Vertexshadern verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="dc164-113">FVF vertex buffers can be used with vertex shaders.</span></span> <span data-ttu-id="dc164-114">Außerdem können Scheitelpunkt Datenströme die gleichen Scheitelpunkt Formate darstellen, die nicht-Sdn-Vertex-Puffer haben können.</span><span class="sxs-lookup"><span data-stu-id="dc164-114">Also, vertex streams can represent the same vertex formats that non-FVF vertex buffers can.</span></span> <span data-ttu-id="dc164-115">Sie müssen nicht zum Eingeben von Daten aus separaten Scheitelpunkt Puffern verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="dc164-115">They do not have to be used to input data from separate vertex buffers.</span></span> <span data-ttu-id="dc164-116">Die zusätzliche Flexibilität der neuen Scheitelpunkt Datenströme ermöglicht Anwendungen, die Ihre Daten getrennt halten müssen, damit Sie besser funktionieren, aber dies ist nicht zwingend erforderlich.</span><span class="sxs-lookup"><span data-stu-id="dc164-116">The additional flexibility of the new vertex streams enables applications that need to keep their data separate to work better, but it is not required.</span></span> <span data-ttu-id="dc164-117">Wenn die Anwendung verschachtelte Daten im Voraus beibehalten kann, ist dies eine Leistungssteigerung.</span><span class="sxs-lookup"><span data-stu-id="dc164-117">If the application can maintain interleaved data in advance, then that is a performance boost.</span></span> <span data-ttu-id="dc164-118">Wenn die Anwendung nur die Daten vor jedem Renderingbefehl interverlässt, sollte Sie die API oder Hardware für mehrere Streams aktivieren.</span><span class="sxs-lookup"><span data-stu-id="dc164-118">If the application will only interleave the data before every rendering call, then it should enable the API or hardware to do this with multiple streams.</span></span>

<span data-ttu-id="dc164-119">Die wichtigste Aufgabe bei der Scheitelpunkt Leistung besteht darin, einen 32-Byte-Scheitelpunkt zu verwenden und eine gute Cache Reihenfolge zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="dc164-119">The most important things with vertex performance is to use a 32-byte vertex, and to maintain good cache ordering.</span></span>

## <a name="related-topics"></a><span data-ttu-id="dc164-120">Zugehörige Themen</span><span class="sxs-lookup"><span data-stu-id="dc164-120">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="dc164-121">Vertex-Puffer</span><span class="sxs-lookup"><span data-stu-id="dc164-121">Vertex Buffers</span></span>](vertex-buffers.md)
</dt> </dl>

 

 
