---
title: Verwenden des ADSI-Schemas
description: Ein Schema definiert das Universum von Objekten, die in einem Verzeichnis gespeichert sind.
ms.assetid: 140a5dd0-6f50-4f84-8708-45c0f1c6bdc4
ms.tgt_platform: multiple
keywords:
- Verwenden des ADSI-Schemas
- ADSI ADSI, using, ADSI-Schema
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 478104a24d850f79cc52473f3d9e546936c56650
ms.sourcegitcommit: b0ebdefc3dcd5c04bede94091833aa1015a2f95c
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 08/21/2020
ms.locfileid: "103858475"
---
# <a name="using-the-adsi-schema"></a><span data-ttu-id="610ec-105">Verwenden des ADSI-Schemas</span><span class="sxs-lookup"><span data-stu-id="610ec-105">Using the ADSI Schema</span></span>

<span data-ttu-id="610ec-106">Ein Schema definiert das Universum von Objekten, die in einem Verzeichnis gespeichert sind.</span><span class="sxs-lookup"><span data-stu-id="610ec-106">A schema defines the universe of objects stored in a directory.</span></span> <span data-ttu-id="610ec-107">In Active Directory gibt das Schema an, welche Attribute ein Verzeichnisdienst Objekt haben kann oder muss.</span><span class="sxs-lookup"><span data-stu-id="610ec-107">In Active Directory, the schema specifies what attributes a directory service object can, or must, have.</span></span> <span data-ttu-id="610ec-108">Außerdem gibt Sie den Wertebereich und die Syntax der Attribute an und gibt an, ob Sie einzelne oder mehrere Werte unterstützen.</span><span class="sxs-lookup"><span data-stu-id="610ec-108">It also specifies the value range and the syntax of the attributes and whether they support single or multiple values.</span></span> <span data-ttu-id="610ec-109">Kurz gesagt, ist das Schema nach Klassendefinitionen, Attribut Definitionen und Attribut Syntax angeordnet.</span><span class="sxs-lookup"><span data-stu-id="610ec-109">In short, the schema is organized by class definitions, attribute definitions, and attribute syntax.</span></span> <span data-ttu-id="610ec-110">ADSI bietet drei Schnittstellen zum Lesen von Attribut-, Klassen-und Syntax Daten aus einem Schema: [**iadsclass**](/windows/desktop/api/Iads/nn-iads-iadsclass), [**iadsproperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty)und [**iadssyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax).</span><span class="sxs-lookup"><span data-stu-id="610ec-110">ADSI provides three interfaces for reading attribute, class, and syntax data from a schema: [**IADsClass**](/windows/desktop/api/Iads/nn-iads-iadsclass), [**IADsProperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty), and [**IADsSyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax).</span></span>

<span data-ttu-id="610ec-111">Active Directory verwendet einen Satz von Schema Objekten, um eine dynamisch erweiterbare Schema Verwaltung bereitzustellen.</span><span class="sxs-lookup"><span data-stu-id="610ec-111">Active Directory uses a set of schema objects to provide dynamically extensible schema management.</span></span> <span data-ttu-id="610ec-112">Weitere Informationen zu einem unbekannten Objekt finden Sie in den zugehörigen Schema Objekten.</span><span class="sxs-lookup"><span data-stu-id="610ec-112">For more information about an unknown object, look up its associated schema objects.</span></span> <span data-ttu-id="610ec-113">Um eine neue Klassendefinition zu erstellen oder eine vorhandene Klassendefinition zu erweitern, können Sie die entsprechenden Schema Objekte erstellen oder erweitern.</span><span class="sxs-lookup"><span data-stu-id="610ec-113">To create a new class definition or extend an existing class definition, you can create or extend the appropriate schema objects.</span></span> <span data-ttu-id="610ec-114">Schema Objekte werden im Schema Container eines bestimmten Verzeichnisses organisiert.</span><span class="sxs-lookup"><span data-stu-id="610ec-114">Schema objects are organized in the schema container of a given directory.</span></span> <span data-ttu-id="610ec-115">Verwenden Sie zum Zugreifen auf eine Objekt Schema Klasse die [**IADs. Schema**](iads-property-methods.md) -Eigenschaft des-Objekts, um die ADsPath-Zeichenfolge abzurufen, und verwenden Sie diese Zeichenfolge, um eine Bindung an eine [**iadsclass**](/windows/desktop/api/Iads/nn-iads-iadsclass) -Schnittstelle in der Objekt Schema Klasse herzustellen.</span><span class="sxs-lookup"><span data-stu-id="610ec-115">To access an object schema class, use the [**IADs.Schema**](iads-property-methods.md) property of the object to obtain the ADsPath string and use that string to bind to an [**IADsClass**](/windows/desktop/api/Iads/nn-iads-iadsclass) interface on the object schema class.</span></span>

<span data-ttu-id="610ec-116">Um Attribut Definitionen, d. h. den Wertebereich, die Syntax usw., zu bestimmen, überprüfen Sie die Schema Attribut Objekte für jede Eigenschaft, die vom Verzeichnisdienst Objekt unterstützt wird.</span><span class="sxs-lookup"><span data-stu-id="610ec-116">To determine attribute definitions, that is, the range of values, the syntax, and so on, inspect the schema attribute objects for each property supported by the directory service object.</span></span> <span data-ttu-id="610ec-117">Weitere Informationen zum Zugreifen auf die Schema Attribut Objekte finden Sie unter [**iadsproperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty).</span><span class="sxs-lookup"><span data-stu-id="610ec-117">For more information about how to access the schema attribute objects, see [**IADsProperty**](/windows/desktop/api/Iads/nn-iads-iadsproperty).</span></span>

<span data-ttu-id="610ec-118">ADSI abstrahiert die Syntax Daten bei Bedarf und ermöglicht die Verwendung von [**iadssyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax) , um die Syntax zu identifizieren, die für die Darstellung von Objektdaten erforderlich ist.</span><span class="sxs-lookup"><span data-stu-id="610ec-118">ADSI abstracts the syntax data as necessary and enables you to use [**IADsSyntax**](/windows/desktop/api/Iads/nn-iads-iadssyntax) to identify the syntax required to represent object data.</span></span>

<span data-ttu-id="610ec-119">Weitere Informationen zum Active Directory Schema finden Sie unter [Active Directory Schema](/windows/desktop/AD/active-directory-schema).</span><span class="sxs-lookup"><span data-stu-id="610ec-119">For more information about the Active Directory schema, see [Active Directory Schema](/windows/desktop/AD/active-directory-schema).</span></span> <span data-ttu-id="610ec-120">Codebeispiele, die zum Lesen des Schema Containers verwendet werden, finden Sie unter [Lesen des Schemas](reading-the-schema.md).</span><span class="sxs-lookup"><span data-stu-id="610ec-120">For code examples to use to read the schema container, see [Reading the Schema](reading-the-schema.md).</span></span>

 

 