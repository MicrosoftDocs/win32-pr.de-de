---
description: 'Weitere Informationen finden Sie hier: Extensible Storage Engine'
title: Erweiterbare Speicher-Engine
TOCTitle: Extensible Storage Engine
ms:assetid: 5c485eff-4329-4dc1-aa45-fb66e6554792
ms:mtpsurl: https://msdn.microsoft.com/library/Gg269259(v=EXCHG.10)
ms:contentKeyID: 32765561
ms.date: 04/11/2016
ms.topic: article
ms.openlocfilehash: 0ef85de696f52d2220c11b05ed7ada76a70df871
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 01/07/2021
ms.locfileid: "104041915"
---
# <a name="extensible-storage-engine"></a><span data-ttu-id="06c86-103">Erweiterbare Speicher-Engine</span><span class="sxs-lookup"><span data-stu-id="06c86-103">Extensible Storage Engine</span></span>


<span data-ttu-id="06c86-104">_**Gilt für:** Windows | Windows Server_</span><span class="sxs-lookup"><span data-stu-id="06c86-104">_**Applies to:** Windows | Windows Server_</span></span>

## <a name="extensible-storage-engine"></a><span data-ttu-id="06c86-105">Erweiterbare Speicher-Engine</span><span class="sxs-lookup"><span data-stu-id="06c86-105">Extensible Storage Engine</span></span>

<span data-ttu-id="06c86-106">Beim Extensible Storage Engine (ESE) handelt es sich um eine erweiterte ISAM (Sequential Access Method)-Speichertechnologie.</span><span class="sxs-lookup"><span data-stu-id="06c86-106">The Extensible Storage Engine (ESE) is an advanced indexed and sequential access method (ISAM) storage technology.</span></span> <span data-ttu-id="06c86-107">ESE ermöglicht Anwendungen das Speichern und Abrufen von Daten aus Tabellen mit indizierter oder sequenzieller Cursor Navigation.</span><span class="sxs-lookup"><span data-stu-id="06c86-107">ESE enables applications to store and retrieve data from tables using indexed or sequential cursor navigation.</span></span> <span data-ttu-id="06c86-108">Es unterstützt denormalisierte Schemas, einschließlich Breite Tabellen mit vielen sparsespalten, mehrwertigen Spalten und sparsespalten und umfangreichen Indizes.</span><span class="sxs-lookup"><span data-stu-id="06c86-108">It supports denormalized schemas including wide tables with numerous sparse columns, multi-valued columns, and sparse and rich indexes.</span></span> <span data-ttu-id="06c86-109">Dadurch können Anwendungen einen konsistenten Daten Zustand mithilfe von transaktiven Datenaktualisierungen und-Abruf nutzen.</span><span class="sxs-lookup"><span data-stu-id="06c86-109">It enables applications to enjoy a consistent data state using transacted data update and retrieval.</span></span> <span data-ttu-id="06c86-110">Ein Mechanismus zur Wiederherstellung von abstürzen wird bereitgestellt, damit die Datenkonsistenz auch bei einem Systemabsturz gewahrt bleibt.</span><span class="sxs-lookup"><span data-stu-id="06c86-110">A crash recovery mechanism is provided so that data consistency is maintained even in the event of a system crash.</span></span> <span data-ttu-id="06c86-111">Es bietet ACID-Transaktionen (atomarische konsistente isolierte dauerhafte Transaktionen) über Daten und Schemas über ein Write-Ahead-Protokoll und ein Snapshot-Isolations Modell.</span><span class="sxs-lookup"><span data-stu-id="06c86-111">It provides ACID (Atomic Consistent Isolated Durable) transactions over data and schema by way of a write-ahead log and a snapshot isolation model.</span></span> <span data-ttu-id="06c86-112">Transaktionen in ESE sind stark gleichzeitig, sodass ESE für Server Anwendungen nützlich ist.</span><span class="sxs-lookup"><span data-stu-id="06c86-112">Transactions in ESE are highly concurrent, making ESE useful for server applications.</span></span> <span data-ttu-id="06c86-113">Daten werden zwischengespeichert, um den Leistungsdaten Zugriff zu maximieren.</span><span class="sxs-lookup"><span data-stu-id="06c86-113">It caches data to maximize high performance access to data.</span></span> <span data-ttu-id="06c86-114">Außerdem ist es sehr einfach, was für Anwendungen nützlich ist, die in zusätzlichen Rollen eingesetzt werden.</span><span class="sxs-lookup"><span data-stu-id="06c86-114">In addition, it is lightweight, making it useful for applications that serve in auxiliary roles.</span></span>

<span data-ttu-id="06c86-115">ESE dient der Verwendung in Anwendungen, die eine schnelle und/oder einfache strukturierte Datenspeicherung erfordern, bei der der Rohdatendatei-Zugriff oder die Registrierung die Indizierungs-oder Datengrößen Anforderungen der Anwendung nicht unterstützt.</span><span class="sxs-lookup"><span data-stu-id="06c86-115">ESE is for use in applications that require fast and/or light structured data storage, where raw file access or the registry does not support the application's indexing or data size requirements.</span></span>

<span data-ttu-id="06c86-116">Sie wird von Anwendungen verwendet, die nie mehr als 1 Megabyte an Daten speichern und in Anwendungen mit Datenbanken verwendet werden, die in Extremfällen über 1 Terabyte und in der Regel über 50 Gigabytes verfügen.</span><span class="sxs-lookup"><span data-stu-id="06c86-116">It is used by applications that never store more than 1 megabyte of data, and has been used in applications with databases in extreme cases in excess of 1 terabyte, and commonly over 50 gigabytes.</span></span>

<span data-ttu-id="06c86-117">Diese Dokumentation richtet sich an Entwickler, die mit C und C++ vertraut sind, sowie grundlegende Datenbankkonzepte wie Tabellen, Spalten, Indizes, Wiederherstellungen und Transaktionen.</span><span class="sxs-lookup"><span data-stu-id="06c86-117">This documentation is intended for developers who are familiar with C and C++, and basic database concepts such as tables, columns, indexes, recovery, and transactions.</span></span> <span data-ttu-id="06c86-118">Die einzige Zugriffsmethode für ESE ist die C-API, die in dieser Dokumentation beschrieben wird.</span><span class="sxs-lookup"><span data-stu-id="06c86-118">The only access method for ESE is the C API that is described in this documentation.</span></span>

<span data-ttu-id="06c86-119">Die erweiterbare Speicher-Engine ist eine Windows-Komponente, die in Windows 2000 eingeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="06c86-119">The Extensible Storage Engine is a Windows component that was introduced in Windows 2000.</span></span> <span data-ttu-id="06c86-120">Nicht alle Features oder APIs sind in allen Versionen der Windows-Betriebssysteme verfügbar.</span><span class="sxs-lookup"><span data-stu-id="06c86-120">Not all features or APIs are available in all versions of the Windows operating systems.</span></span>

<span data-ttu-id="06c86-121">ESE bietet eine Benutzermodus-Speicher-Engine, die Daten in flachen Binärdateien verwaltet, auf die über die Windows-APIs zugegriffen werden kann.</span><span class="sxs-lookup"><span data-stu-id="06c86-121">ESE provides a user-mode storage engine that manages data inside of flat, binary files that are accessible through the Windows APIs.</span></span> <span data-ttu-id="06c86-122">Der Zugriff auf ESE erfolgt über eine DLL, die direkt in den Prozess der Anwendung geladen wird. Es sind keine Remote Zugriffsmethoden erforderlich, die von der Datenbank-Engine selbst bereitgestellt werden.</span><span class="sxs-lookup"><span data-stu-id="06c86-122">ESE is accessed through a DLL that is loaded directly into the application's process; no remote access methods are required of or provided by the database engine itself.</span></span> <span data-ttu-id="06c86-123">Obwohl ESE über keine Remote-oder prozessübergreifende Zugriffsmethode verfügt, können die verwendeten Datendateien mithilfe von SMB (Server Message Block) über die Windows-APIs Remote bereitgestellt werden, dies wird jedoch nicht empfohlen.</span><span class="sxs-lookup"><span data-stu-id="06c86-123">Though ESE has no remote or inter-process access method, the data files it uses can be provided remotely by using server message block (SMB) through the Windows APIs, but this is not recommended.</span></span>

<span data-ttu-id="06c86-124">**Hinweis**  Windows XP 64-Bit Edition ist mit Windows Server 2003 identisch, um den unterstützten ESE-Funktions Satz zu bestimmen.</span><span class="sxs-lookup"><span data-stu-id="06c86-124">**Note**  Windows XP 64-Bit Edition is the same as Windows Server 2003 for the purpose of determining the ESE feature set that is supported.</span></span>

### <a name="notes"></a><span data-ttu-id="06c86-125">Notizen</span><span class="sxs-lookup"><span data-stu-id="06c86-125">Notes</span></span>

<span data-ttu-id="06c86-126">ESE wurde früher als Joint Engine Technology (Jet) Blue bezeichnet. häufig wird der Begriff "Jet Blue" oder "Jet" mit dem Begriff "ESE" außerhalb dieser Dokumentation austauschbar verwendet.</span><span class="sxs-lookup"><span data-stu-id="06c86-126">ESE was formerly known as Joint Engine Technology (JET) Blue, and so frequently the term "JET Blue" or "JET" is used interchangeably with the term ESE outside this documentation.</span></span> <span data-ttu-id="06c86-127">Es gibt jedoch tatsächlich zwei vollständig getrennte Implementierungen der Jet-API mit dem Namen Jet Blue und Jet Red.</span><span class="sxs-lookup"><span data-stu-id="06c86-127">However, there are in fact two completely separate implementations of the JET API, called JET Blue and JET Red.</span></span> <span data-ttu-id="06c86-128">Der Begriff "Jet" wird häufig auch verwendet, um auf Jet Red zu verweisen. dabei handelt es sich um die Datenbank-Engine, die mit Microsoft Office Access verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="06c86-128">The term "JET" is frequently also used to refer to JET Red, which is the database engine that is used with Microsoft Office Access.</span></span> <span data-ttu-id="06c86-129">Die beiden Jet-Implementierungen unterscheiden sich vollständig, werden separat gewartet, haben eine sehr andere Funktions Reihe und sind nicht austauschbar.</span><span class="sxs-lookup"><span data-stu-id="06c86-129">The two JET implementations are completely different, are separately maintained, have a vastly different feature set, and are not interchangeable.</span></span> <span data-ttu-id="06c86-130">In der ESE-Dokumentation bezieht sich "Jet" auf die ESE-oder Jet-API, wenn Sie von ESE implementiert wird.</span><span class="sxs-lookup"><span data-stu-id="06c86-130">Within the ESE documentation, "JET" refers to the ESE or the JET API as ESE implements it.</span></span> <span data-ttu-id="06c86-131">Alle Verweise auf das Jet Red werden immer explizit als "Jet Red" bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="06c86-131">Any references to the JET Red will always explicitly be labeled "JET Red".</span></span>

### <a name="in-this-section"></a><span data-ttu-id="06c86-132">In diesem Abschnitt</span><span class="sxs-lookup"><span data-stu-id="06c86-132">In This Section</span></span>

[<span data-ttu-id="06c86-133">Extensible Storage Engine-Referenz</span><span class="sxs-lookup"><span data-stu-id="06c86-133">Extensible Storage Engine Reference</span></span>](./extensible-storage-engine-reference.md)
