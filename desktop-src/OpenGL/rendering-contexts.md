---
title: Renderingkontexte
description: Ein OpenGL-renderingkontext ist ein Port, über den alle OpenGL-Befehle bestanden werden. Jeder Thread, der OpenGL-Aufrufe ausführt, muss über einen aktuellen renderingkontext verfügen. Renderingkontexte verknüpfen OpenGL mit den Windows-windowingsystemen.
ms.assetid: 9fbbb0be-2db4-4bfc-9a5c-a43d71554abc
keywords:
- OpenGL unter Windows, renderingkontexte
- Rendern von Kontexten OpenGL
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 53ac2ac6c5948da2b7372d600377666cd9e4e074
ms.sourcegitcommit: 2d531328b6ed82d4ad971a45a5131b430c5866f7
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 09/16/2019
ms.locfileid: "104310532"
---
# <a name="rendering-contexts"></a><span data-ttu-id="62895-107">Renderingkontexte</span><span class="sxs-lookup"><span data-stu-id="62895-107">Rendering Contexts</span></span>

<span data-ttu-id="62895-108">Ein OpenGL- *renderingkontext* ist ein Port, über den alle OpenGL-Befehle bestanden werden.</span><span class="sxs-lookup"><span data-stu-id="62895-108">An OpenGL *rendering context* is a port through which all OpenGL commands pass.</span></span> <span data-ttu-id="62895-109">Jeder Thread, der OpenGL-Aufrufe ausführt, muss über einen aktuellen renderingkontext verfügen.</span><span class="sxs-lookup"><span data-stu-id="62895-109">Every thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="62895-110">Renderingkontexte verknüpfen OpenGL mit den Windows-windowingsystemen.</span><span class="sxs-lookup"><span data-stu-id="62895-110">Rendering contexts link OpenGL to the Windows windowing systems.</span></span>

<span data-ttu-id="62895-111">Eine Anwendung gibt einen Windows-Gerätekontext an, wenn ein renderingkontext erstellt wird.</span><span class="sxs-lookup"><span data-stu-id="62895-111">An application specifies a Windows device context when it creates a rendering context.</span></span> <span data-ttu-id="62895-112">Dieser renderingkontext eignet sich zum Zeichnen auf dem Gerät, auf das der angegebene Gerätekontext verweist.</span><span class="sxs-lookup"><span data-stu-id="62895-112">This rendering context is suitable for drawing on the device that the specified device context references.</span></span> <span data-ttu-id="62895-113">Insbesondere hat der renderingkontext das gleiche Pixel Format wie der Gerätekontext.</span><span class="sxs-lookup"><span data-stu-id="62895-113">In particular, the rendering context has the same pixel format as the device context.</span></span> <span data-ttu-id="62895-114">Weitere Informationen finden Sie unter [Rendern von Kontextfunktionen](rendering-context-functions.md).</span><span class="sxs-lookup"><span data-stu-id="62895-114">For more information, see [Rendering Context Functions](rendering-context-functions.md).</span></span>

<span data-ttu-id="62895-115">Trotz dieser Beziehung ist ein renderingkontext nicht mit einem Gerätekontext identisch.</span><span class="sxs-lookup"><span data-stu-id="62895-115">Despite this relationship, a rendering context is not the same as a device context.</span></span> <span data-ttu-id="62895-116">Ein Gerätekontext enthält Informationen, die für die Grafik Komponente (GDI) von Windows relevant sind.</span><span class="sxs-lookup"><span data-stu-id="62895-116">A device context contains information pertinent to the graphics component (GDI) of Windows.</span></span> <span data-ttu-id="62895-117">Ein renderingkontext enthält Informationen, die für OpenGL relevant sind.</span><span class="sxs-lookup"><span data-stu-id="62895-117">A rendering context contains information pertinent to OpenGL.</span></span> <span data-ttu-id="62895-118">Ein Gerätekontext muss explizit in einem GDI-Befehl angegeben werden.</span><span class="sxs-lookup"><span data-stu-id="62895-118">A device context must be explicitly specified in a GDI call.</span></span> <span data-ttu-id="62895-119">Ein renderingkontext ist in einem OpenGL-Befehl implizit.</span><span class="sxs-lookup"><span data-stu-id="62895-119">A rendering context is implicit in an OpenGL call.</span></span> <span data-ttu-id="62895-120">Vor dem Erstellen eines renderingkontexts sollten Sie das Pixel Format eines Geräte Kontexts festlegen.</span><span class="sxs-lookup"><span data-stu-id="62895-120">You should set a device context's pixel format before creating a rendering context.</span></span>

<span data-ttu-id="62895-121">Ein Thread, der OpenGL-Aufrufe ausführt, muss über einen aktuellen renderingkontext verfügen.</span><span class="sxs-lookup"><span data-stu-id="62895-121">A thread that makes OpenGL calls must have a current rendering context.</span></span> <span data-ttu-id="62895-122">Wenn eine Anwendung OpenGL-Aufrufe von einem Thread durchführt, der keinen aktuellen renderingkontext hat, geschieht nichts. der-Befehl hat keine Auswirkung.</span><span class="sxs-lookup"><span data-stu-id="62895-122">If an application makes OpenGL calls from a thread that lacks a current rendering context, nothing happens; the call has no effect.</span></span> <span data-ttu-id="62895-123">Eine Anwendung erstellt in der Regel einen renderingkontext, legt sie als aktuellen renderingkontext eines Threads fest und ruft dann OpenGL-Funktionen auf.</span><span class="sxs-lookup"><span data-stu-id="62895-123">An application commonly creates a rendering context, sets it as a thread's current rendering context, and then calls OpenGL functions.</span></span> <span data-ttu-id="62895-124">Wenn der Aufruf von OpenGL-Funktionen abgeschlossen ist, entkoppelt die Anwendung den renderingkontext aus dem Thread und löscht dann den renderingkontext.</span><span class="sxs-lookup"><span data-stu-id="62895-124">When it finishes calling OpenGL functions, the application uncouples the rendering context from the thread, and then deletes the rendering context.</span></span> <span data-ttu-id="62895-125">Ein Fenster kann mehrere renderingkontexte gleichzeitig zeichnen, aber ein Thread kann nur über einen aktuellen aktiven renderingkontext verfügen.</span><span class="sxs-lookup"><span data-stu-id="62895-125">A window can have multiple rendering contexts drawing to it at one time, but a thread can have only one current, active rendering context.</span></span>

<span data-ttu-id="62895-126">Ein aktueller renderingkontext verfügt über einen zugeordneten Gerätekontext.</span><span class="sxs-lookup"><span data-stu-id="62895-126">A current rendering context has an associated device context.</span></span> <span data-ttu-id="62895-127">Dieser Gerätekontext muss nicht der gleiche Gerätekontext sein, den er beim Erstellen des renderingkontexts verwendet hat, er muss jedoch auf dasselbe Gerät verweisen und das gleiche Pixel Format aufweisen.</span><span class="sxs-lookup"><span data-stu-id="62895-127">That device context need not be the same device context as that used when the rendering context was created, but it must reference the same device and have the same pixel format.</span></span>

<span data-ttu-id="62895-128">Ein Thread kann nur über einen aktuellen renderingkontext verfügen.</span><span class="sxs-lookup"><span data-stu-id="62895-128">A thread can have only one current rendering context.</span></span> <span data-ttu-id="62895-129">Ein renderingkontext kann nur auf einen Thread aktuell sein.</span><span class="sxs-lookup"><span data-stu-id="62895-129">A rendering context can be current to only one thread.</span></span>

 

 




