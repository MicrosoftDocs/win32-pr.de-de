---
description: Komponenten sind häufig so konzipiert, dass Sie Initialisierungs Aufgaben ausführen, wenn Sie zum ersten Mal aufgerufen werden, anstatt Sie zu laden.
ms.assetid: 404c083c-7bee-44c2-b8e7-da1901b6ab2f
title: One-Time Initialisierung
ms.topic: article
ms.date: 05/31/2018
ms.openlocfilehash: 16f451e3c51716b4ff6f33b55d8d8602b5d5c28f
ms.sourcegitcommit: 831e8f3db78ab820e1710cede244553c70e50500
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 01/08/2021
ms.locfileid: "106352566"
---
# <a name="one-time-initialization"></a><span data-ttu-id="e565e-103">One-Time Initialisierung</span><span class="sxs-lookup"><span data-stu-id="e565e-103">One-Time Initialization</span></span>

<span data-ttu-id="e565e-104">Komponenten sind häufig so konzipiert, dass Sie Initialisierungs Aufgaben ausführen, wenn Sie zum ersten Mal aufgerufen werden, anstatt Sie zu laden.</span><span class="sxs-lookup"><span data-stu-id="e565e-104">Components are often designed to perform initialization tasks when they are first called, rather than when they are loaded.</span></span> <span data-ttu-id="e565e-105">Die einmaligen Initialisierungs Funktionen stellen sicher, dass diese Initialisierung nur einmal erfolgt, auch wenn mehrere Threads die Initialisierung möglicherweise versuchen.</span><span class="sxs-lookup"><span data-stu-id="e565e-105">The one-time initialization functions ensure that this initialization occurs only once, even when multiple threads may attempt the initialization.</span></span>

<span data-ttu-id="e565e-106">**Windows Server 2003 und Windows XP:** Anwendungen müssen eine eigene Synchronisierung für die einmalige Initialisierung mithilfe der [Interlocked-Funktionen](interlocked-variable-access.md) oder anderer Synchronisierungs Mechanismen bereitstellen.</span><span class="sxs-lookup"><span data-stu-id="e565e-106">**Windows Server 2003 and Windows XP:** Applications must provide their own synchronization for one-time initialization by using the [interlocked functions](interlocked-variable-access.md) or other synchronization mechanism.</span></span> <span data-ttu-id="e565e-107">Die einmaligen Initialisierungs Funktionen sind ab Windows Vista und Windows Server 2008 verfügbar.</span><span class="sxs-lookup"><span data-stu-id="e565e-107">The one-time initialization functions are available starting with Windows Vista and Windows Server 2008.</span></span>

<span data-ttu-id="e565e-108">Die einmaligen Initialisierungs Funktionen bieten bedeutende Vorteile, um sicherzustellen, dass nur ein Thread die Initialisierung ausführt:</span><span class="sxs-lookup"><span data-stu-id="e565e-108">The one-time initialization functions provide significant advantages to ensure that only one thread performs the initialization:</span></span>

-   <span data-ttu-id="e565e-109">Sie sind für die Geschwindigkeit optimiert.</span><span class="sxs-lookup"><span data-stu-id="e565e-109">They are optimized for speed.</span></span>
-   <span data-ttu-id="e565e-110">Sie erstellen die entsprechenden Barrieren für Prozessorarchitekturen, die Sie benötigen.</span><span class="sxs-lookup"><span data-stu-id="e565e-110">They create the appropriate barriers on processor architectures that require them.</span></span>
-   <span data-ttu-id="e565e-111">Sie unterstützen die gesperrte und parallele Initialisierung.</span><span class="sxs-lookup"><span data-stu-id="e565e-111">They support both locked and parallel initialization.</span></span>
-   <span data-ttu-id="e565e-112">Sie vermeiden interne sperren, sodass der Code asynchron oder synchron ausgeführt werden kann.</span><span class="sxs-lookup"><span data-stu-id="e565e-112">They avoid internal locking so the code can operate asynchronously or synchronously.</span></span>

<span data-ttu-id="e565e-113">Das System verwaltet den Initialisierungs Prozess über eine nicht transparente **Init \_ Once** -Struktur, die Daten und Zustandsinformationen enthält.</span><span class="sxs-lookup"><span data-stu-id="e565e-113">The system manages the initialization process through an opaque **INIT\_ONCE** structure that contains data and state information.</span></span> <span data-ttu-id="e565e-114">Der Aufrufer ordnet diese Struktur zu und initialisiert Sie, indem entweder [**InitOnceInitialize**](/windows/win32/api/synchapi/nf-synchapi-initonceinitialize) aufgerufen wird (um die Struktur dynamisch zu initialisieren) oder die Konstante **Init \_ Once \_ static \_ Init** der Struktur Variablen zuzuweisen (um die Struktur statisch zu initialisieren).</span><span class="sxs-lookup"><span data-stu-id="e565e-114">The caller allocates this structure and initializes it by either calling [**InitOnceInitialize**](/windows/win32/api/synchapi/nf-synchapi-initonceinitialize) (to initialize the structure dynamically) or assigning the constant **INIT\_ONCE\_STATIC\_INIT** to the structure variable (to initialize the structure statically).</span></span> <span data-ttu-id="e565e-115">Anfänglich sind die in der einmaligen Initialisierungs Struktur gespeicherten Daten NULL, und Ihr Status ist nicht initialisiert.</span><span class="sxs-lookup"><span data-stu-id="e565e-115">Initially, the data stored in the one-time initialization structure is NULL and its state is uninitialized.</span></span>

<span data-ttu-id="e565e-116">Einmalige Initialisierungs Strukturen können nicht Prozess übergreifend gemeinsam verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="e565e-116">One-time initialization structures cannot be shared across processes.</span></span>

<span data-ttu-id="e565e-117">Der Thread, der die Initialisierung ausführt, kann optional einen Kontext festlegen, der für den Aufrufer verfügbar ist, nachdem die Initialisierung beendet wurde.</span><span class="sxs-lookup"><span data-stu-id="e565e-117">The thread that performs the initialization can optionally set a context that is available to the caller after initialization is complete.</span></span> <span data-ttu-id="e565e-118">Der Kontext kann ein Synchronisierungs Objekt sein, oder es kann ein Wert oder eine Datenstruktur sein.</span><span class="sxs-lookup"><span data-stu-id="e565e-118">The context can be a synchronization object or it can be a value or data structure.</span></span> <span data-ttu-id="e565e-119">Wenn der Kontext ein Wert ist, muss die von der **\_ nach Ordnung \_ \_ reservierten \_ Bits** mit niedriger Reihenfolge nach dem Wert 0 (null) sein.</span><span class="sxs-lookup"><span data-stu-id="e565e-119">If the context is a value, its low-order **INIT\_ONCE\_CTX\_RESERVED\_BITS** must be zero.</span></span> <span data-ttu-id="e565e-120">Wenn der Kontext eine Datenstruktur ist, muss die Datenstruktur **DWORD**-ausgerichtet sein.</span><span class="sxs-lookup"><span data-stu-id="e565e-120">If the context is a data structure, the data structure must be **DWORD**-aligned.</span></span> <span data-ttu-id="e565e-121">Der Kontext wird im *lpContext* -Ausgabeparameter der [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) -oder [**InitOnceExecuteOnce**](/windows/win32/api/synchapi/nf-synchapi-initonceexecuteonce) -Funktion an den Aufrufer zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="e565e-121">The context is returned to the caller in the *lpContext* output parameter of the [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) or [**InitOnceExecuteOnce**](/windows/win32/api/synchapi/nf-synchapi-initonceexecuteonce) function.</span></span>

<span data-ttu-id="e565e-122">Die einmalige Initialisierung kann synchron oder asynchron ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="e565e-122">One-time initialization can be performed synchronously or asynchronously.</span></span> <span data-ttu-id="e565e-123">Eine optionale Rückruffunktion kann für die synchrone einmalige Initialisierung verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="e565e-123">An optional callback function can be used for synchronous one-time initialization.</span></span>

## <a name="synchronous-one-time-initialization"></a><span data-ttu-id="e565e-124">Synchrone einmalige Initialisierung</span><span class="sxs-lookup"><span data-stu-id="e565e-124">Synchronous One-time Initialization</span></span>

<span data-ttu-id="e565e-125">In den folgenden Schritten wird die synchrone einmalige Initialisierung beschrieben, bei der keine Rückruffunktion verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="e565e-125">The following steps describe synchronous one-time initialization that does not use a callback function.</span></span>

1.  <span data-ttu-id="e565e-126">Der erste Thread, der die [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) -Funktion aufruft, bewirkt, dass die einmalige Initialisierung beginnt.</span><span class="sxs-lookup"><span data-stu-id="e565e-126">The first thread to call the [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) function successfully causes one-time initialization to begin.</span></span> <span data-ttu-id="e565e-127">Für die synchrone einmalige Initialisierung muss **InitOnceBeginInitialize** ohne das " **Init \_ Once \_ Async** "-Flag aufgerufen werden.</span><span class="sxs-lookup"><span data-stu-id="e565e-127">For synchronous one-time initialization, **InitOnceBeginInitialize** must be called without the **INIT\_ONCE\_ASYNC** flag.</span></span>
2.  <span data-ttu-id="e565e-128">Nachfolgende Threads, die versuchen, eine Initialisierung durchführen, werden blockiert, bis der erste Thread entweder die Initialisierung abschließt oder</span><span class="sxs-lookup"><span data-stu-id="e565e-128">Subsequent threads that attempt initialization are blocked until the first thread either completes initialization or fails.</span></span> <span data-ttu-id="e565e-129">Wenn der erste Thread ausfällt, kann der nächste Thread versuchen, die Initialisierung durchführen, usw.</span><span class="sxs-lookup"><span data-stu-id="e565e-129">If the first thread fails, the next thread is allowed to attempt the initialization, and so on.</span></span>
3.  <span data-ttu-id="e565e-130">Wenn die Initialisierung abgeschlossen ist, ruft der Thread die [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) -Funktion auf.</span><span class="sxs-lookup"><span data-stu-id="e565e-130">When initialization is finished, the thread calls the [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) function.</span></span> <span data-ttu-id="e565e-131">Der Thread kann optional ein Synchronisierungs Objekt (oder andere Kontext Daten) erstellen und im *lpContext* -Parameter der **InitOnceComplete** -Funktion angeben.</span><span class="sxs-lookup"><span data-stu-id="e565e-131">The thread can optionally create a synchronization object (or other context data) and specify it in the *lpContext* parameter of the **InitOnceComplete** function.</span></span>
4.  <span data-ttu-id="e565e-132">Wenn die Initialisierung erfolgreich ist, wird der Zustand der einmaligen Initialisierungs Struktur in initialisiert geändert, und das *lpContext* -handle (sofern vorhanden) wird in der Initialisierungs Struktur gespeichert.</span><span class="sxs-lookup"><span data-stu-id="e565e-132">If the initialization succeeds, the state of the one-time initialization structure is changed to initialized and the *lpContext* handle (if any) is stored in the initialization structure.</span></span> <span data-ttu-id="e565e-133">Bei nachfolgenden Initialisierungs versuchen werden diese Kontext Daten zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="e565e-133">Subsequent initialization attempts return this context data.</span></span> <span data-ttu-id="e565e-134">Wenn die Initialisierung fehlschlägt, sind die Daten **null**.</span><span class="sxs-lookup"><span data-stu-id="e565e-134">If the initialization fails, the data is **NULL**.</span></span>

<span data-ttu-id="e565e-135">In den folgenden Schritten wird die synchrone einmalige Initialisierung beschrieben, die eine Rückruffunktion verwendet.</span><span class="sxs-lookup"><span data-stu-id="e565e-135">The following steps describe synchronous one-time initialization that uses a callback function.</span></span>

1.  <span data-ttu-id="e565e-136">Der erste Thread, der die [**InitOnceExecuteOnce**](/windows/win32/api/synchapi/nf-synchapi-initonceexecuteonce) -Funktion erfolgreich aufruft, übergibt einen Zeiger auf eine Anwendungs definierte [*InitOnceCallback*](/windows/win32/api/synchapi/nc-synchapi-pinit_once_fn) -Rückruffunktion und alle Daten, die von der Rückruffunktion benötigt werden.</span><span class="sxs-lookup"><span data-stu-id="e565e-136">The first thread to successfully call the [**InitOnceExecuteOnce**](/windows/win32/api/synchapi/nf-synchapi-initonceexecuteonce) function passes a pointer to an application-defined [*InitOnceCallback*](/windows/win32/api/synchapi/nc-synchapi-pinit_once_fn) callback function and any data required by the callback function.</span></span> <span data-ttu-id="e565e-137">Wenn der-Vorgang erfolgreich ist, wird die *InitOnceCallback* -Rückruffunktion ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="e565e-137">If the call succeeds, the *InitOnceCallback* callback function executes.</span></span>
2.  <span data-ttu-id="e565e-138">Nachfolgende Threads, die versuchen, eine Initialisierung durchführen, werden blockiert, bis der erste Thread entweder die Initialisierung abschließt oder</span><span class="sxs-lookup"><span data-stu-id="e565e-138">Subsequent threads that attempt initialization are blocked until the first thread either completes initialization or fails.</span></span> <span data-ttu-id="e565e-139">Wenn der erste Thread ausfällt, kann der nächste Thread versuchen, die Initialisierung durchführen, usw.</span><span class="sxs-lookup"><span data-stu-id="e565e-139">If the first thread fails, the next thread is allowed to attempt the initialization, and so on.</span></span>
3.  <span data-ttu-id="e565e-140">Wenn die Initialisierung abgeschlossen ist, gibt die Rückruffunktion zurück.</span><span class="sxs-lookup"><span data-stu-id="e565e-140">When initialization is finished, the callback function returns.</span></span> <span data-ttu-id="e565e-141">Die Rückruffunktion kann optional ein Synchronisierungs Objekt (oder andere Kontext Daten) erstellen und in seinem *Kontext* Ausgabeparameter angeben.</span><span class="sxs-lookup"><span data-stu-id="e565e-141">The callback function can optionally create a synchronization object (or other context data) and specify it in its *Context* output parameter.</span></span>
4.  <span data-ttu-id="e565e-142">Wenn die Initialisierung erfolgreich ist, wird der Zustand der einmaligen Initialisierungs Struktur in initialisiert geändert, und das *Kontext* handle (sofern vorhanden) wird in der Initialisierungs Struktur gespeichert.</span><span class="sxs-lookup"><span data-stu-id="e565e-142">If the initialization succeeds, the state of the one-time initialization structure is changed to initialized and the *Context* handle (if any) is stored in the initialization structure.</span></span> <span data-ttu-id="e565e-143">Bei nachfolgenden Initialisierungs versuchen werden diese Kontext Daten zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="e565e-143">Subsequent initialization attempts return this context data.</span></span> <span data-ttu-id="e565e-144">Wenn die Initialisierung fehlschlägt, sind die Daten **null**.</span><span class="sxs-lookup"><span data-stu-id="e565e-144">If the initialization fails, the data is **NULL**.</span></span>

## <a name="asynchronous-one-time-initialization"></a><span data-ttu-id="e565e-145">Asynchrone einmalige Initialisierung</span><span class="sxs-lookup"><span data-stu-id="e565e-145">Asynchronous One-time Initialization</span></span>

<span data-ttu-id="e565e-146">In den folgenden Schritten wird die asynchrone einmalige Initialisierung beschrieben.</span><span class="sxs-lookup"><span data-stu-id="e565e-146">The following steps describe asynchronous one-time initialization.</span></span>

1.  <span data-ttu-id="e565e-147">Wenn mehrere Threads gleichzeitig versuchen, die Initialisierung durch Aufrufen von [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) with **Init \_ Once \_ Async** zu starten, ist die Funktion für alle Threads erfolgreich, wobei der Parameter *fPending* auf **true** festgelegt ist.</span><span class="sxs-lookup"><span data-stu-id="e565e-147">If multiple threads simultaneously attempt to begin initialization by calling [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) with **INIT\_ONCE\_ASYNC**, the function succeeds for all of the threads with the *fPending* parameter set to **TRUE**.</span></span> <span data-ttu-id="e565e-148">Nur ein einziger Thread wird bei der Initialisierung tatsächlich erfolgreich ausgeführt. andere gleichzeitige Versuche ändern den Initialisierungs Status nicht.</span><span class="sxs-lookup"><span data-stu-id="e565e-148">Only one thread will actually succeed at initialization; other concurrent attempts do not change the initialization state.</span></span>
2.  <span data-ttu-id="e565e-149">Wenn [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) zurückgegeben wird, gibt der Parameter " *fPending* " den Initialisierungs Status an:</span><span class="sxs-lookup"><span data-stu-id="e565e-149">When [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) returns, the *fPending* parameter indicates the initialization status:</span></span>
    -   <span data-ttu-id="e565e-150">Wenn " *fPending* " den Wert " **false**" aufweist, wurde ein Thread erfolgreich initialisiert.</span><span class="sxs-lookup"><span data-stu-id="e565e-150">If *fPending* is **FALSE**, one thread has succeeded at initialization.</span></span> <span data-ttu-id="e565e-151">Andere Threads sollten alle Kontext Daten bereinigen, die Sie erstellt haben, und die Kontext Daten im *lpContext* -Ausgabeparameter von [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize)verwenden.</span><span class="sxs-lookup"><span data-stu-id="e565e-151">Other threads should clean up any context data they have created and use the context data in the *lpContext* output parameter of [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize).</span></span>
    -   <span data-ttu-id="e565e-152">Wenn " *Ausstehend* " den Wert " **true**" aufweist, wurde die Initialisierung noch nicht abgeschlossen und andere Threads sollten fortgesetzt</span><span class="sxs-lookup"><span data-stu-id="e565e-152">If *fPending* is **TRUE**, initialization has not yet completed and other threads should continue.</span></span>
3.  <span data-ttu-id="e565e-153">Jeder Thread ruft die [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) -Funktion auf.</span><span class="sxs-lookup"><span data-stu-id="e565e-153">Each thread calls the [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) function.</span></span> <span data-ttu-id="e565e-154">Der Thread kann optional ein Synchronisierungs Objekt (oder andere Kontext Daten) erstellen und im *lpContext* -Parameter von **InitOnceComplete** angeben.</span><span class="sxs-lookup"><span data-stu-id="e565e-154">The thread can optionally create a synchronization object (or other context data) and specify it in the *lpContext* parameter of **InitOnceComplete**.</span></span>
4.  <span data-ttu-id="e565e-155">Wenn [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) zurückgibt, gibt der Rückgabewert an, ob der aufrufende Thread bei der Initialisierung erfolgreich war.</span><span class="sxs-lookup"><span data-stu-id="e565e-155">When [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) returns, its return value indicates whether the calling thread succeeded at initialization.</span></span>
    -   <span data-ttu-id="e565e-156">Wenn [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) erfolgreich ist, ist der aufrufende Thread bei der Initialisierung erfolgreich.</span><span class="sxs-lookup"><span data-stu-id="e565e-156">If [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) succeeds, the calling thread has succeeded at initialization.</span></span> <span data-ttu-id="e565e-157">Der Zustand der einmaligen Initialisierungs Struktur wird in initialisiert geändert, und das *lpContext* -handle (sofern vorhanden) wird in der Initialisierungs Struktur gespeichert.</span><span class="sxs-lookup"><span data-stu-id="e565e-157">The state of the one-time initialization structure is changed to initialized and the *lpContext* handle (if any) is stored in the initialization structure.</span></span>
    -   <span data-ttu-id="e565e-158">Wenn [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) fehlschlägt, wurde ein anderer Thread erfolgreich initialisiert.</span><span class="sxs-lookup"><span data-stu-id="e565e-158">If [**InitOnceComplete**](/windows/win32/api/synchapi/nf-synchapi-initoncecomplete) fails, another thread has succeeded at initialization.</span></span> <span data-ttu-id="e565e-159">Der aufrufende Thread sollte alle Kontext Daten bereinigen, die er erstellt hat, und [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) mit init aufrufen, um die in der einmaligen Initialisierungs Struktur gespeicherten Kontext Daten abzurufen. **\_ \_ \_**</span><span class="sxs-lookup"><span data-stu-id="e565e-159">The calling thread should clean up any context data it has created and call [**InitOnceBeginInitialize**](/windows/win32/api/synchapi/nf-synchapi-initoncebegininitialize) with **INIT\_ONCE\_CHECK\_ONLY** to retrieve any context data stored in the one-time initialization structure.</span></span>

## <a name="calling-one-time-initialization-from-multiple-sites"></a><span data-ttu-id="e565e-160">Aufrufen One-Time Initialisierung von mehreren Standorten</span><span class="sxs-lookup"><span data-stu-id="e565e-160">Calling One-Time Initialization from multiple sites</span></span>

<span data-ttu-id="e565e-161">Die einmalige Initialisierung, die durch eine einzelne **Init \_ Once** -Struktur geschützt wird, kann von mehreren Standorten aus ausgeführt werden. es kann ein anderer Rückruf von jeder Site und die Synchronisierung mit und ohne Rückruf gemischt werden.</span><span class="sxs-lookup"><span data-stu-id="e565e-161">One-time initialization guarded by a single **INIT\_ONCE** structure may be performed from mutiple sites; different callback may be passed from each site, and synchronization with and without callback may be mixed.</span></span> <span data-ttu-id="e565e-162">Die Initialisierung wird immer noch für die gleichzeitige Durchführung garantiert.</span><span class="sxs-lookup"><span data-stu-id="e565e-162">Initialization is still guaranted to perform sucesfully just once.</span></span>

<span data-ttu-id="e565e-163">Die asynchrone und synchrone Initialisierung kann jedoch nicht gemischt werden: Wenn versucht wird, die asynchrone Initialisierung zu starten, treten beim Versuch, die synchrone Initialisierung zu starten, Fehler</span><span class="sxs-lookup"><span data-stu-id="e565e-163">However, asynchronous and synchronous initialization cannot be mixed: once asynchronous initialization is attempted, attempts to start synchronous initialization would fail.</span></span>

## <a name="related-topics"></a><span data-ttu-id="e565e-164">Zugehörige Themen</span><span class="sxs-lookup"><span data-stu-id="e565e-164">Related topics</span></span>

<dl> <dt>

[<span data-ttu-id="e565e-165">Verwenden der One-Time Initialisierung</span><span class="sxs-lookup"><span data-stu-id="e565e-165">Using One-Time Initialization</span></span>](using-one-time-initialization.md)
</dt> </dl>

 

 
